---
id: agt-040
difficulty: L4
category: ai-agent
subcategory: 记忆系统
tags:
- RAG
- Agent
feynman:
  essence: 综合相关性、时间与重要性，从海量记忆中召回最匹配的上下文。
  analogy: 像搜索引擎，不仅看词匹配，还看时效和热度。
  first_principle: 如何从记忆库中找出对当前决策最有用的信息子集？
  key_points:
  - 单一检索策略（仅向量或仅时间）存在盲区。
  - 需多因子融合并动态调整权重。
  - 召回后需去重、重排并限制 Token 长度。
memory_points:
- 核心策略：单一策略不足，需混合时间、相关性、重要性多因子打分。
- Generative Agents 公式：Score = w1*相关性 + w2*近期性 + w3*重要性。
- 近期性计算：使用时间衰减函数（如 e^-αΔt），越近权重越高。
- 实战避坑：Top-K 结果可能重复，使用 MMR 算法保证结果多样性。
- 限长处理：按得分排序后进行 Token 装箱，或分层注入（摘要+细节）。
---

# 记忆检索策略

检索策略决定在生成下一轮回复前，从记忆池中召回哪些信息。单一策略往往不足，需混合使用。

### 常见策略
1.  **基于时间**：优先召回最近对话。适合排障、联调等强时效任务。
2.  **基于相关性**：通过向量相似度召回。适合用户换说法的开放域问答。
3.  **基于重要性**：优先召回高权重信息（如用户核心目标）。避免早期硬约束被淹没。

### Generative Agents 多因子融合
斯坦福 Generative Agents 论文提出综合打分公式：
$$ Score = w_{rel} \cdot \widehat{rel} + w_{rec} \cdot \widehat{recency} + w_{imp} \cdot \widehat{importance} $$
其中：
*   $\widehat{rel}$: 相关性（向量余弦）
*   $\widehat{recency}$: 近期性（时间衰减函数，如 $e^{-\alpha \Delta t}$）
*   $\widehat{importance}$: 重要性（通常由 LLM 生成时打分 1-10）

### 实现难点
*   **去重**：同一事实的不同表述需语义对齐，避免冗余占用上下文。
*   **限长**：按得分排序后进行 Token 装箱，或分层注入（摘要+细节）。

### 架构图：多路召回与融合流程
```text
                    Query
                      │
      ┌───────────────┼───────────────┐
      ▼               ▼               ▼
┌───────────┐   ┌───────────┐   ┌───────────┐
│ Vector    │   │ Time/Rec  │   │ Knowledge │
│ Search    │   │ Filter    │   │ Graph     │
└─────┬─────┘   └─────┬─────┘   └─────┬─────┘
      │               │               │
      ▼               ▼               ▼
┌──────────────────────────────────────┐
│         Scoring & Reranking          │
│  (Apply weights: rel, rec, imp)      │
└──────────────────┬───────────────────┘
                   │
                   ▼
┌──────────────────────────────────────┐
│          Token Packing (Bin Packing) │
│  (Fit top K into Context Window)     │
└──────────────────┬───────────────────┘
                   │
                   ▼
            Context Injection
```

### 实战案例
*   **优化经验**：在客服场景中，直接按 Score 返回 Top 5 可能导致 5 条都是关于“退款政策”的重复内容，而忽略了用户当前具体的“订单号”。实战中常加入 **MMR (Maximal Marginal Relevance)** 算法，在保证相关性的同时，强制增加结果之间的多样性（如 1 条政策 + 1 条历史记录 + 1 条工具日志）。

### 对比表格：召回策略
| 策略 | 核心逻辑 | 适用场景 | 常用权重配置 |
| :--- | :--- | :--- | :--- |
| **时间优先** | 时间倒序 | 连续任务、纠错对话 | Recency=1.0, Rel=0.2 |
| **语义优先** | 向量相似度 TopK | 知识库问答 | Rel=1.0, Recency=0.1 |
| **重要性优先** | 加权打分 | 长目标追踪 | Imp=0.6, Rec=0.2, Rel=0.2 |
| **混合策略** | 多因子加权 | 通用智能体 | Rel=0.5, Rec=0.3, Imp=0.2 |

### 代码逻辑（Python）
```python
import numpy as np

def hybrid_score(memories, query_vec, weights={'rel': 0.5, 'rec': 0.3, 'imp': 0.2}, time_decay=0.1):
    now = time.time()
    scored_memories = []
    
    for m in memories:
        # 1. 归一化相关得分
        rel_score = np.dot(m.vector, query_vec) / (np.linalg.norm(m.vector) * np.linalg.norm(query_vec))
        
        # 2. 计算时间衰减 (例如：半衰期策略)
        rec_score = np.exp(-time_decay * (now - m.timestamp))
        
        # 3. 获取归一化重要性 (假设写入时已存 0-1 之间)
        imp_score = m.importance
        
        # 4. 加权求和
        final_score = (
            weights['rel'] * rel_score + 
            weights['rec'] * rec_score + 
            weights['imp'] * imp_score
        )
        scored_memories.append({'memory': m, 'score': final_score})
        
    # 按分数降序排列
    return sorted(scored_memories, key=lambda x: x['score'], reverse=True)
```


## 记忆要点

- 核心策略：单一策略不足，需混合时间、相关性、重要性多因子打分。
- Generative Agents 公式：Score = w1*相关性 + w2*近期性 + w3*重要性。
- 近期性计算：使用时间衰减函数（如 e^-αΔt），越近权重越高。
- 实战避坑：Top-K 结果可能重复，使用 MMR 算法保证结果多样性。
- 限长处理：按得分排序后进行 Token 装箱，或分层注入（摘要+细节）。

## 结构化回答

**30 秒电梯演讲：** 记忆检索不能只看相关性——像搜索引擎还要看时效和热度。Generative Agents 的经典公式是 Score = 相关性 + 近期性 + 重要性三因子加权。纯 Top-K 会返回一堆重复内容，必须配 MMR 算法保证多样性，再用 Token 装箱限制长度。

**展开框架：**
1. **单一策略有盲区** — 纯时间优先漏语义相关、纯相关性漏时效、纯重要性淹没近期细节，必须混合。
2. **三因子融合公式** — Score = w1·相关性（向量余弦）+ w2·近期性（时间衰减 e^-αΔt）+ w3·重要性（LLM 打分 1-10），权重按场景调。
3. **MMR 保多样性** — Top-K 常返回重复内容（5 条都是退款政策），MMR 在相关性和多样性间权衡，强制结果不雷同。
4. **Token 装箱限长** — 按得分排序后做 Token 装箱塞进上下文窗口，或分层注入（摘要+细节）。

**收尾：** 我做过客服，纯 Score 返回 Top 5 全是退款政策重复内容，加 MMR 后变成 1 条政策+1 条历史+1 条日志，覆盖面广多了。您想深入聊三因子权重、MMR 算法还是 Token 装箱？

## 视频脚本

> 预计时长：3 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：记忆检索策略 | "记忆检索不能只看相关性，像搜索引擎还要看时效和热度。" | 开场钩子 |
| 0:25 | 搜索引擎多因子类比 | "像搜索引擎，不仅看词匹配，还看时效和热度。单一策略有盲区。" | 本质类比 |
| 0:55 | 三因子融合公式 | "Generative Agents 公式：Score = 相关性 + 近期性 + 重要性。近期性用时间衰减 e^-αΔt。" | 核心公式 |
| 1:35 | MMR 保多样性示意 | "Top-K 会返回重复内容，5 条都是退款政策。MMR 在相关性和多样性间权衡，强制不雷同。" | MMR 去重 |
| 2:10 | Token 装箱限长 | "按得分排序后 Token 装箱塞进上下文窗口，或分层注入摘要加细节。" | 限长处理 |
| 2:45 | 总结卡 | "记住：三因子混合、MMR 保多样、装箱限长。下期讲记忆更新。" | 收尾 |

