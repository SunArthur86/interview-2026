---
id: sar-006
difficulty: L2
category: ai-agent
feynman:
  essence: 扩展RAG至文本、图像等多模态数据的联合检索与生成。
  analogy: 像全能图书管理员，能同时管理文字书和图片集。
  first_principle: 如何让系统理解和关联不同模态（文本、图像）的信息？
  key_points:
  - CLIP实现图文向量对齐
  - 支持文本搜图或图文混合检索
  - 利用多模态LLM（如GPT-4V）理解内容
  - 需解决多模态分块和对齐难题
memory_points:
- 检索指标：Context Precision（排序精度）、Context Recall（召回率）、MRR/NDCG
- 生成指标：Faithfulness（忠实度/防幻觉）、Answer Relevance（答案相关性）
- 评估框架：RAGAS（LLM-as-a-Judge）、TruLens（RAG Triad三角评估）
- 核心痛点：Faithfulness检测幻觉，Context Recall检测检索遗漏
---

# RAG系统评估有哪些指标？如何系统地评估RAG质量？

RAG系统的评估通常分为三个核心维度：检索质量、生成质量和端到端质量。

### 1. 检索阶段评估 
评估检索到的上下文是否相关、准确。

*   **Context Precision (上下文精度)**：检索结果中相关文档的排序情况。相关文档是否排在前面？计算公式通常参考平均精度。
*   **Context Recall (上下文召回)**：标准答案中引用的信息，有多少被包含在检索到的上下文中。即检索是否有遗漏。
*   **MRR (Mean Reciprocal Rank)**：第一个相关文档出现在排序第几位。倒数排名越高越好。
*   **NDCG (Normalized Discounted Cumulative Gain)**：考虑排序位置和相关性等级的指标，关注高相关性文档是否排在前列。

### 2. 生成阶段评估 
评估LLM基于上下文生成的答案质量。

*   **Faithfulness (忠实度)**：答案是否严格依据检索到的上下文。**这是RAG评估的核心，用于检测幻觉。**
*   **Answer Relevance (答案相关性)**：生成的答案是否解决了用户的问题。

### 3. 端到端评估 (End-to-End Evaluation)
*   **RAGAS**：最流行的开源框架。流程如下：
    ```text
    ┌──────────┐     ┌──────────────┐     ┌──────────────┐
    │ Question │ ──> │ LLM Generate │ ──> │  LLM Verify  │
    └──────────┘     │   Answer     │     │  (Is it      │
                     └──────┬───────┘     │  Faithful?)  │
                            │             └──────┬───────┘
                            v                    │
                     ┌──────────────┐            v
                     │   Context    │     ┌──────────────┐
                     │   (Retrieved)│     │  Score (0-1) │
                     └──────────────┘     └──────────────┘
    ```
*   **TruLens (RAG Triad)**：由Context Relevance、Faithfulness、Answer Relevance三个指标组成三角评估。
*   **ARES**：基于对抗训练的评估方法，无需人工标注，通过构造噪声数据来评估。

### 4. 系统性能评估
*   **Latency (延迟)**：首字生成时间 (TTFT) 和总生成时间。
*   **Throughput (吞吐量)**：单位时间内处理的请求数。

💡 **实战案例**：某在线客服RAG系统上线后，用户投诉"机器人答非所问"。通过引入RAGAS评估发现Faithfulness得分仅0.6。排查发现，检索到的Top-1文档是"相关但不完全匹配"的旧款产品说明，导致LLM强行编造。优化检索策略（引入混合检索）后，Faithfulness提升至0.92，投诉率下降70%。

```python
# 伪代码：使用 RAGAS 框架计算 Faithfulness 分数
# 语言：Python

from ragas import evaluate
from ragas.metrics import faithfulness, answer_relevancy

def calculate_faithfulness(question, answer, contexts):
    # 构造 RAGAS 的 Dataset 格式
    dataset = [{
        "question": question,
        "answer": answer,
        "contexts": contexts
    }]
    
    # 运行评估
    result = evaluate(
        dataset=dataset,
        metrics=[faithfulness] # 专注于忠实度/幻觉检测
    )
    
    # result 通常是一个 0-1 之间的分数
    return result["faithfulness"]
```

## 常见考点
1. **如何在没有标准答案的情况下评估？**
   使用 LLM-as-a-Judge 模式，利用强模型（如GPT-4）根据Prompt生成的答案与检索Context的一致性来打分，不依赖人工标注的标准答案。
2. **RAGAS 中 Context Recall 和 Faithfulness 的区别？**
   Recall 关注检索阶段是否“找全了”（相对标准答案），Faithfulness 关注生成阶段是否“瞎编了”（相对检索Context）。
3. **如何解决评估中的分数波动问题？**
   设置多个样本或使用更强的一致性Prompt，必要时引入人工抽检（Human-in-the-loop）。


## 记忆要点

- 检索指标：Context Precision（排序精度）、Context Recall（召回率）、MRR/NDCG
- 生成指标：Faithfulness（忠实度/防幻觉）、Answer Relevance（答案相关性）
- 评估框架：RAGAS（LLM-as-a-Judge）、TruLens（RAG Triad三角评估）
- 核心痛点：Faithfulness检测幻觉，Context Recall检测检索遗漏

## 结构化回答

**30 秒电梯演讲：** 评估 RAG 要分三段看：检索阶段看 Context Precision/Recall 和 MRR/NDCG，生成阶段看 Faithfulness 和 Answer Relevance，端到端用 RAGAS 或 TruLens 框架跑。最该盯死的是 Faithfulness——它直接检测幻觉，答案是不是基于检索到的上下文编的。

**展开框架：**
1. **检索阶段** — Context Precision 看相关文档排没排前面，Context Recall 看该找的找全没，MRR/NDCG 衡量排序质量。
2. **生成阶段** — Faithfulness 检测答案是否忠实于上下文（防幻觉核心），Answer Relevance 看答案是否切题。
3. **评估框架** — RAGAS 用 LLM-as-a-Judge 不依赖人工标注，TruLens 的 RAG Triad 三角评估（上下文相关性、忠实度、答案相关性）。

**收尾：** 我在一个客服 RAG 上线后用 RAGAS 测出 Faithfulness 只有 0.6，排查发现 Top-1 是旧款说明导致 LLM 强行编造，优化混合检索后提到 0.92，投诉降了 70%。您想聊 RAGAS 怎么搭测试集，还是 LLM-as-a-Judge 的分数波动怎么解决？

## 视频脚本

> 预计时长：2 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：RAG 怎么评估 | "RAG 系统好不好，不能光看答得顺，得拆成检索和生成两段分别打分。" | 开场钩子 |
| 0:15 | 三段评估维度图 | "分三段：检索看 Precision/Recall/MRR，生成看 Faithfulness 和相关性，端到端跑框架。" | 评估维度 |
| 0:45 | Context Recall vs Faithfulness 对比 | "Recall 看检索找全没，Faithfulness 看生成瞎编没——一个查遗漏，一个查幻觉。" | 核心区分 |
| 1:10 | RAGAS 流程图 | "RAGAS 用 LLM-as-a-Judge：生成答案后再让强模型验证是否忠实上下文，输出 0-1 分。" | RAGAS 框架 |
| 1:35 | 客服 Faithfulness 0.6→0.92 案例 | "实战：客服 RAG 投诉多，RAGAS 测出 Faithfulness 0.6，发现 Top-1 是旧说明，优化后到 0.92。" | 实战案例 |
| 1:55 | 总结卡 | "记住：Faithfulness 查幻觉，Context Recall 查遗漏，RAGAS 一键跑。" | 收尾 |

