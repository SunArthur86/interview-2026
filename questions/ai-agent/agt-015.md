---
id: agt-015
difficulty: L3
category: ai-agent
subcategory: Agent核心框架
tags:
- Agent
feynman:
  essence: 通过多角色分工协作，模拟团队工作流程解决复杂任务。
  analogy: 像组建公司团队，有研发、测试、产品经理，大家互相配合把活干好。
  first_principle: 如何通过角色专业化与协作机制提升复杂任务解决的质量与鲁棒性？
  key_points:
  - 通过 System Prompt 定义角色职责
  - 利用对话或流水线模式进行协作
  - 分工可提高质量但增加沟通成本
  - 需引入外部验证防止群体迷思
memory_points:
- 核心设计：多角色Agent协作，模拟人类团队，分工明确（如写手vs审核）。
- 协作模式：对话驱动（群聊式）或流程驱动（流水线式）。
- 优势：专家模型降本增效，并行探索多路径，角色互补提高质量。
- 挑战：Token消耗指数级增长，易死循环，可能出现社会惰性（互相附和）。
- 防附和：引入批评者角色、外部工具验证、设置最大轮次或终止Token。
---

# AutoGen / CrewAI 等多 Agent 框架

### 1. 概念解释
多 Agent 框架（如 AutoGen, CrewAI）将任务分配给多个不同“角色”的 Agent。它们通过对话或消息传递协作完成任务，模拟人类团队的协作方式。

### 2. 核心设计
- **角色定义**：每个 Agent 有特定的 System Prompt（设定背景和目标）、工具集和配置（如温度参数）。
- **协作模式**：
  - **对话驱动**：像群聊一样互相发送消息，基于内容决定谁下一个发言。
  - **流程驱动**：像流水线一样按顺序传递工作，一个 Agent 的输出是另一个的输入。

### 3. 多 Agent 交互架构 (ASCII 图示)

```text
         模式 A: 对话驱动          模式 B: 层级/流水线

   +--------+                      +--------+
   | User   |                      | User   |
   +---+----+                      +---+----+
       |                              |
       v                              v
   +-------+  Msg    +-------+    +-------+   Task   +-------+
   |Agent A+-------->+Agent B+    |Manager|--------->|Agent 1|
   +-------+         +-------+    +-------+          +-------+
       ^                 |            ^                 |
       |      Msg        |            |  Result         | Result
       +-----------------+            |                 v
                                     |             +-------+
                                     +-------------+Agent 2|
                                                   +-------+
```

### 4. 优势与挑战
- **优势**：
  - **分工明确**：如写代码 Agent vs 审查代码 Agent，利用角色互补提高质量。
  - **专家模型**：不同的 Agent 可以指定使用不同的 LLM（如一个用 GPT-4 规划，一个用 GPT-3.5 摘要），优化成本与性能。
  - **并行探索**：多个 Agent 可以同时尝试不同的解决路径。
- **挑战**：
  - **协调成本高**：Token 消耗随 Agent 数量指数级增长。
  - **社会惰性**：可能出现“互相附和”或推诿。
  - **死循环**：没有明确的终止条件时，对话可能无限进行。

### 5. 防止“互相附和”与死循环的策略
- **角色设置**：引入独立的**批评者** 或 **魔鬼代言人** 角色，专门负责挑错。
- **外部验证**：强制要求使用外部工具验证（如单元测试、代码执行结果、RAG 检索证据），而不只是互相采信。
- **结构化对话**：使用 Speaker Selection 机制（基于 LLM 预测下一个发言者）或 Group Chat Manager 控制轮次。
- **终止条件**：设置最大轮次，或者定义特定的终止 Token（如 "TERMINATE"）。

### 6. 实战深化
#### 6.1 实战案例
在构建自动发帖系统时，如果只有“写手”和“审核员”，审核员往往倾向于通过（社会惰性）。引入“律师”Agent 后，律师会专门审查内容是否有版权风险或违规词。这种三足鼎立的架构显著降低了内容事故率，且多 Agent 间的辩论往往能单 Agent 遗漏的边缘情况。

#### 6.2 关键代码示例

```python
# AutoGen 伪代码：配置多 Agent 辩论模式
from autogen import AssistantAgent, UserProxyAgent, GroupChat, GroupChatManager

# 定义角色：包含 Devil's Advocate (批评者)
coder = AssistantAgent(name="Coder", system_message="You write Python code.")
reviewer = AssistantAgent(name="Reviewer", system_message="You review code for bugs.")
critic = AssistantAgent(name="Critic", system_message="You are a devil's advocate. Find logic flaws and security risks.")

# 定义群聊管理器，控制发言顺序
groupchat = GroupChat(agents=[coder, reviewer, critic], messages=[], max_round=10)
manager = GroupChatManager(groupchat=groupchat, name="Manager")

# 启动任务
user_proxy = UserProxyAgent(name="User", human_input_mode="NEVER")
user_proxy.initiate_chat(
    manager, 
    message="Write a secure function to process user input."
)
```


## 记忆要点

- 核心设计：多角色Agent协作，模拟人类团队，分工明确（如写手vs审核）。
- 协作模式：对话驱动（群聊式）或流程驱动（流水线式）。
- 优势：专家模型降本增效，并行探索多路径，角色互补提高质量。
- 挑战：Token消耗指数级增长，易死循环，可能出现社会惰性（互相附和）。
- 防附和：引入批评者角色、外部工具验证、设置最大轮次或终止Token。

## 结构化回答

**30 秒电梯演讲：** AutoGen、CrewAI 这类多 Agent 框架，就是给任务配一支"AI 团队"——用 System Prompt 定义不同角色（写手、审核、批评者），让他们对话或流水线协作。分工能提高质量，但代价是 Token 指数级增长，还容易出现"互相附和"的社会惰性。

**展开框架：**
1. **角色定义** — 每个 Agent 有专属 System Prompt、工具集和模型配置（比如规划用 GPT-4、摘要用 GPT-3.5 降本）。
2. **两种协作模式** — 对话驱动（群聊式，基于内容决定谁发言）；流程驱动（流水线式，A 的输出是 B 的输入）。
3. **三大挑战** — Token 消耗随 Agent 数指数增长；没有终止条件会死循环；最隐蔽的是"社会惰性"——互相附和走过场。
4. **防附和四招** — 引入魔鬼代言人角色专门挑错、强制外部工具验证（单测/检索）、Speaker Selection 控制发言、设最大轮次或 TERMINATE token。

**收尾：** 我做过自动发帖系统，光有写手和审核员时审核员总放水，加了"律师"Agent 专查版权风险后事故率显著下降。您想深入聊角色设计、协作模式还是终止条件？

## 视频脚本

> 预计时长：4 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：多 Agent 框架 | "给任务配一支 AI 团队，这就是 AutoGen、CrewAI 干的事。" | 开场钩子 |
| 0:20 | 多角色协作图：写手-审核-批评者 | "核心是用 System Prompt 定义角色，让他们对话或流水线协作。分工能提质量。" | 角色定义 |
| 0:55 | 对话驱动 vs 流水线驱动对比 | "两种模式：群聊式基于内容决定谁发言；流水线式 A 的输出是 B 的输入。" | 协作模式 |
| 1:35 | 社会惰性示意图：互相点赞 | "最大坑是社会惰性——互相附和走过场。Token 还会随 Agent 数指数增长。" | 三大挑战 |
| 2:10 | 防附和四招清单 | "防附和四招：魔鬼代言人挑错、外部工具验证、Speaker Selection 控发言、设最大轮次。" | 防附和 |
| 2:50 | 自动发帖系统加律师 Agent 案例 | "实战：发帖系统加了'律师'Agent 专查版权风险，事故率显著下降。" | 实战案例 |
| 3:30 | 总结卡 | "记住：角色分工、两种模式、防附和四招。下期讲框架综合题。" | 收尾 |

