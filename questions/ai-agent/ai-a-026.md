---
id: ai-a-026
difficulty: L3
category: ai-agent
subcategory: 多Agent系统
tags:
- IO
- Elasticsearch
feynman:
  essence: 由Manager协调的多人多角色对话框架。
  analogy: 像公司开会，经理控制流程决定谁发言。
  first_principle: 如何协调多个智能体在同一个对话中有序协作？
  key_points:
  - GroupChatManager负责调度发言顺序
  - 支持自动、轮询、随机等多种选人策略
  - 适合多角色协作或专家辩论场景
follow_up:
- auto选择策略的准确率如何?
- 如何防止Agent间死循环对话?
memory_points:
- 核心架构：GroupChatManager管理Agent列表与发言顺序，维护共享上下文。
- 选择策略：Auto（LLM裁判高耗）、RoundRobin（固定轮询低耗）、Manual（人工介入）。
- 实战痛点：防止死循环需设置max_round，或引入Admin/自定义函数强制切换发言人。
- 关键参数：max_round限制轮数防死循环，termination_msg定义终止关键词。
---

# AutoGen的GroupChat如何工作?发言人选择策略有哪些

- **AutoGen GroupChat架构:**

多个Agent在一个共享对话中协作,由GroupChatManager管理.

```text
       AutoGen GroupChat 交互流

    +-------------------------+
    |   GroupChatManager      |
    | (管理发言顺序 & 上下文) |
    +-----------+-------------+
                | Select Next
       +--------+--------+-------+
       |                 |       |
   [Agent A]         [Agent B] [Agent C]
   (Coder)          (Reviewer) (Tester)
       |                 |       |
       +--------+-------+-------+
                | Broadcast
                v
       [Shared Conversation History]
```

- **核心组件:**
1. **Agents** - 各有角色和工具, `system_message`定义人设
2. **GroupChatManager** - 核心调度器,维护`messages`列表,决定下一轮谁说话
3. **Speaker Selection** - 决定下一个发言者,支持多种策略

- **发言人选择策略:**

| 策略 | 机制 | 适用场景 | 性能消耗 |
| :--- | :--- | :--- | :--- |
| **auto** (默认) | LLM作为裁判，根据对话历史解析 | 动态协商、不确定的流程 | 高 (每轮需额外LLM调用) |
| **round_robin** | 按列表顺序固定轮询 | 固定流水线 (如 写->审->测) | 低 (无计算开销) |
| **manual** | 程序暂停，等待人工输入 | 调试、关键决策介入 | 依赖人 |
| **random** | 随机挑选 | 创意生成、头脑风暴 | 低 |
| **custom** | 自定义Python函数逻辑 | 需要特定规则路由 (如关键词触发) | 中 (取决于函数逻辑) |

- **典型配置:**
```python
manager = GroupChatManager(
groupchat=GroupChat(
agents=[planner, coder, reviewer],
messages=[],
max_round=20,  # 防止死循环
speaker_selection_method="auto"
)
)
```

- **实战案例**: 在代码审查场景中，Coder和Reviewer常陷入“改好-未改好”的死循环。实战中我们会引入带有`termination_msg`的Admin Agent，或自定义选择函数：如果连续3轮发言人相同，强制切换到第三方Agent。

- **代码示例 (自定义选择策略)**:
```python
def custom_speaker_selection(last_speaker, groupchat):
    # 如果发言人是Coder且包含[已修复]，强制切换给Tester
    if last_speaker.name == "Coder" and "[已修复]" in last_speaker.last_message():
        return next(agent for agent in groupchat.agents if agent.name == "Tester")
    return None # 回退到默认auto

manager = GroupChatManager(groupchat=groupchat, speaker_selection_method=custom_speaker_selection)
```

- **适用场景:**
- 代码开发:Planner→Coder→Tester→Reviewer
- 研究任务:Researcher→Analyst→Writer
- 决策:多个专家Agent辩论

## 常见考点
1. **终止条件**：如何定义对话结束？（例如`max_round`、检测到“TERMINATE”关键词）
2. **上下文窗口限制**：多轮对话后History如何处理？（Summary、滑动窗口）
3. **循环问题**：两个Agent陷入无限争论（如Coder说改好了，Reviewer说不行），如何设计机制打破僵局？


## 记忆要点

- 核心架构：GroupChatManager管理Agent列表与发言顺序，维护共享上下文。
- 选择策略：Auto（LLM裁判高耗）、RoundRobin（固定轮询低耗）、Manual（人工介入）。
- 实战痛点：防止死循环需设置max_round，或引入Admin/自定义函数强制切换发言人。
- 关键参数：max_round限制轮数防死循环，termination_msg定义终止关键词。

## 结构化回答

**30 秒电梯演讲：** AutoGen 的 GroupChat 像公司开会——GroupChatManager 当经理控制流程，决定谁发言。选人策略有 Auto（LLM 当裁判）、RoundRobin（固定轮询）、Manual（人工介入）。最大痛点是死循环，靠 max_round 和 termination_msg 兜底。

**展开框架：**
1. **核心架构** — GroupChatManager 管理 Agent 列表与发言顺序，维护共享对话上下文。
2. **选人策略对比** — Auto 用 LLM 裁判动态选人（高耗）、RoundRobin 固定轮询（低耗）、Manual 人工介入、Custom 自定义函数。
3. **防死循环** — 设 max_round 限制轮数，或引入 Admin Agent、自定义函数强制切换发言人打破僵局。

**收尾：** GroupChat 的精髓在选人策略——我可以聊聊怎么用自定义函数解决"改好-未改好"的死循环。

## 视频脚本

> 预计时长：3 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：AutoGen GroupChat | "像公司开会，GroupChatManager 当经理，控制谁发言。" | 类比开场 |
| 0:30 | 架构图（Manager + 多 Agent） | "核心：Manager 管理 Agent 列表和发言顺序，维护共享上下文。" | 核心架构 |
| 1:15 | 四种选人策略对比表 | "Auto 用 LLM 裁判，RoundRobin 固定轮询，Manual 人工，Custom 自定义。" | 选人策略 |
| 2:00 | 死循环案例（改好-未改好） | "防死循环：设 max_round，或引入 Admin 强制切换发言人。" | 防死循环 |
| 2:40 | 自定义选择函数示意 | "实战：连续 3 轮同人发言就强制切换第三方 Agent。" | 实战技巧 |

