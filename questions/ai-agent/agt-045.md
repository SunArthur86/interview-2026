---
id: agt-045
difficulty: L4
category: ai-agent
subcategory: 多智能体
tags:
- ai-agent
feynman:
  essence: 定义智能体间信息传递的底层协议与基础设施。
  analogy: 像人和人交流，可以私聊（直接消息）、贴公告（黑板）、广播（PubSub）或发工单（队列）。
  first_principle: 如何在复杂的分布式系统中高效、可靠地传递信息？
  key_points:
  - 直接消息简单但耦合度高。
  - 黑板适合共享状态的协作求解。
  - 消息队列提供高可靠与异步处理能力。
memory_points:
- 直接消息：点对点，简单易追踪，但 N² 连接复杂度高。
- 共享黑板：公共读写区，解耦通信，适合协作求解，需处理锁冲突。
- 发布-订阅：主题广播，扩展性好，适合事件通知，通常发后即焚。
- 消息队列：FIFO 异步解耦，提供削峰填谷、重试、持久化，适合任务流转。
- 选型建议：强审计用队列，探索协作用黑板，简单交互用直接消息。
---

# 通信机制

通信机制决定 Agent 之间如何交换信息，常见四类：直接消息、共享黑板、发布-订阅、消息队列。

### 核心机制对比
1.  **直接消息**：A 显式发给 B（点对点）。简单易追踪，但 N² 连接复杂度高。
2.  **共享黑板**：公共读写区。解耦了“谁知道谁”，适合协作求解，但并发写需处理锁冲突。
3.  **发布-订阅**：主题广播，订阅者自选。扩展性好，适合事件通知。
4.  **消息队列**：先入先出，异步解耦。提供削峰填谷、重试、持久化能力，适合任务流转。

### 选型建议
*   强审计、强顺序、高吞吐 -> **消息队列**。
*   探索式协作、快速原型 -> **共享黑板**。
*   简单多角色交互 -> **直接消息**。

### 通信拓扑示意图

```text
直接消息 (P2P):             共享黑板:

   (A) ──────> (B)               ┌───────────────────┐
    |                           │                   │
    v                           │    Blackboard     │
   (C)              (D) ───────>│ (Read/Write Data) │<────── (E)
                              │                   │
                              └───────────────────┘

发布-订阅 (Pub/Sub):          消息队列:

   Publisher                     Producer
      │                             │
      v                             v
  ┌─────────┐                   ┌─────┐   ┌─────┐   ┌─────┐
  │  Topic  │                   │Queue│──>│Cons1│   │Cons2│
  └────┬────┘                   └─────┘   └─────┘   └─────┘
       │    Subscribe           (持久化/顺序)
   ┌────┴────┐
   │         │
Sub1      Sub2
```

### 代码逻辑（示意）
```python
class Blackboard:
    def write(self, key, value):
        with self._lock: self._data[key] = value
    def read(self, key): return self._data.get(key)

class PubSub:
    def publish(self, topic, payload):
        for handler in self._subs.get(topic, []):
            handler(topic, payload)
```

### 实战案例
在多模态内容处理流水线中，使用 RabbitMQ 实现队列通信。当图片处理 Agent 崩溃时，消息自动回到 Unacked 状态，重新入队供其他 Worker 消费，成功避免了因单节点故障导致整个长视频处理任务丢失的问题。

### 代码示例（基于 Redis 的简单队列）
```python
import redis
import json

class AgentQueue:
    def __init__(self, queue_name):
        self.r = redis.Redis()
        self.q_name = queue_name

    def send_task(self, agent_name, task_data):
        # 模拟消息投递
        payload = {"target": agent_name, "data": task_data}
        self.r.lpush(self.q_name, json.dumps(payload))

    def receive_task(self):
        # 阻塞式获取（BRPOP）
        _, msg = self.r.brpop(self.q_name)
        return json.loads(msg)
```

### 对比表格
| 机制 | 耦合度 | 可靠性 | 典型应用场景 |
| :--- | :--- | :--- | :--- |
| **直接消息** | 高 | 依赖网络，无持久化 | 少量 Agent，简单请求/响应 |
| **共享黑板** | 低（空间耦合） | 中（需自行处理并发） | 软件工程 IDE 插件、协同编辑 |
| **发布-订阅** | 低 | 低（通常发后即焚） | 实时通知、日志广播 |
| **消息队列** | 低 | 高（持久化、ACK） | 异步任务流、高并发处理 |

## 常见考点
1. **黑板锁机制**：高并发读写黑板时，如何避免脏读？（答：使用乐观锁（版本号）或读写锁；对于 Agent 场景，通常写入频率低于读取，可接受简单的互斥锁）。
2. **消息丢失**：使用 RabbitMQ/Kafka 等队列时，如何保证 Agent 消息不丢失？（答：开启 ACK 确认机制，消费者处理成功后才回执，结合持久化磁盘）。
3. **上下文窗口**：在共享黑板模式中，历史记录过多怎么办？（答：实施「滚动摘要」机制，保留最近 N 轮关键信息，旧信息压缩后归档）。

## 核心知识点图

<img src="/interview-2026/images/diagram_ai-agent_agt-045.svg" alt="通信机制 - 核心知识点图" style="max-width:100%;height:auto;border:1px solid var(--border);border-radius:8px;margin:1em 0;" />

## 记忆要点

- 直接消息：点对点，简单易追踪，但 N² 连接复杂度高。
- 共享黑板：公共读写区，解耦通信，适合协作求解，需处理锁冲突。
- 发布-订阅：主题广播，扩展性好，适合事件通知，通常发后即焚。
- 消息队列：FIFO 异步解耦，提供削峰填谷、重试、持久化，适合任务流转。
- 选型建议：强审计用队列，探索协作用黑板，简单交互用直接消息。

## 结构化回答

**30 秒电梯演讲：** Agent 之间通信有四种方式——直接消息（点对点简单但 N² 连接复杂）、共享黑板（公共读写区解耦但要处理锁）、发布订阅（广播适合事件通知但发后即焚）、消息队列（异步解耦能持久化重试）。选型口诀：强审计用队列，探索协作用黑板，简单交互用直接消息。

**展开框架：**
1. **直接消息** — 点对点显式发送，简单易追踪；缺点是 N 个 Agent 会有 N² 连接，扩展性差。
2. **共享黑板** — 公共读写区解耦"谁知道谁"，适合协作求解和快速原型；并发写要处理锁冲突。
3. **发布订阅** — 主题广播订阅者自选，扩展性好适合事件通知；通常发后即焚可靠性低。
4. **消息队列** — FIFO 异步解耦，提供削峰填谷、重试、持久化，强审计强顺序高吞吐必选。

**收尾：** 我做过多模态处理流水线用 RabbitMQ，图片 Agent 崩溃后消息自动重回队列被其他 Worker 消费，没丢任务。您想深入聊队列可靠性、黑板锁机制还是选型决策？

## 视频脚本

> 预计时长：3 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：多 Agent 通信机制 | "Agent 之间怎么通信？四种方式：直接消息、黑板、发布订阅、消息队列。" | 开场钩子 |
| 0:25 | 私聊/公告/广播/工单类比 | "像人交流：私聊是直接消息，贴公告是黑板，广播是发布订阅，发工单是消息队列。" | 四机制类比 |
| 0:55 | 直接消息 vs 共享黑板对比 | "直接消息点对点简单但 N² 连接复杂；黑板公共读写区解耦但并发写要处理锁。" | 前两种 |
| 1:35 | 发布订阅 vs 消息队列对比 | "发布订阅广播扩展性好但发后即焚；消息队列 FIFO 异步解耦，能持久化重试削峰填谷。" | 后两种 |
| 2:10 | 选型口诀 + RabbitMQ 案例 | "选型口诀：强审计用队列，探索协作用黑板，简单交互用直接消息。RabbitMQ 让崩溃 Agent 的任务自动重回队列。" | 选型+实战 |
| 2:45 | 总结卡 | "记住：四种机制、按场景选型、队列最可靠。下期讲任务分配。" | 收尾 |

