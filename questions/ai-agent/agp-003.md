---
id: agp-003
difficulty: L1
category: ai-agent
subcategory: Prompt工程
feynman:
  essence: 基于数据的闭环迭代
  analogy: 像修车，先确诊问题，再换一个零件试跑，而不是同时换一堆。
  first_principle: 如何科学地优化复杂系统的性能
  key_points:
  - 固定评测基准
  - 分类错误原因
  - 单变量控制修改
  - 量化版本效果
follow_up:
- 迭代 Prompt 时如何避免「过拟合」到几个测试用例？
- A/B 测试 Prompt 的样本量怎么定？
memory_points:
- 迭代闭环：建评测集 -> 错误归因 -> 控制变量修改 -> 回归测试
- 错误分类：理解错、幻觉、格式错、合规问题，对症下药
- 数据驱动：记录版本改动与指标，避免凭直觉盲目调整
- 警惕灾难性遗忘：优化某类 Case 时需全量回归测试
---

# 你如何迭代优化 Prompt

(1) **建立固定评测集**：构建包含典型与边缘案例的 Golden Dataset，并制定量化评分标准（如准确率、格式合规率）；(2) **错误分类分析**：将失败日志归类（理解错误、知识幻觉、格式错误、安全合规问题）；(3) **控制变量迭代**：采用“一次改一个变量”的策略，如单独修改指令、更换 Few-shot 示例或调整分隔符；(4) **数据驱动记录**：记录每个 Prompt 版本的改动内容及对应的评测指标，避免凭直觉调整导致效果波动。

**实战案例**：优化一个电商情感分析 Agent 时，发现“讽刺”识别率极低。通过分析 Bad Cases，发现模型将“这手机真‘好用’，一天充三次电”误判为正面。在 Prompt 中加入“反语/反讽识别模式”的 Few-shot 示例后，该类别 F1 分数从 0.2 提升至 0.85。

**代码示例**：
```python
# 使用框架记录迭代实验（如 LangSmith 或自建）
import json

experiment_log = {
    "version": "v4.1",
    "change_desc": "增加反语识别 Few-shot 示例",
    "metrics": {
        "accuracy": 0.92,
        "sarcasm_recall": 0.85 # 关注特定指标提升
    },
    "prompt_template": system_prompt_v4 # 存储完整快照
}

with open("prompt_experiments.jsonl", "a") as f:
    f.write(json.dumps(experiment_log) + "\n")
```

**优化闭环流程：**
```text
     ┌─────────────┐
     │  现有 Prompt │
     └──────┬──────┘
            │
            ▼
     ┌─────────────┐
     │  运行评测集  │───────┐
     └──────┬──────┘       │ (获取 Bad Cases)
            │             │
            ▼             │
     ┌─────────────┐       │
     │  错误归因分析 │◄──────┘
     └──────┬──────┘
            │
            ▼ (假设生成: 改指令/换样例/加CoT)
     ┌─────────────┐
     │   修改 Prompt │
     └──────┬──────┘
            │
            ▼ (回归测试)
     ┌─────────────┐       是
     │ 指标提升?   ├────────┤ (发布) 
     └──────┬──────┘       │ 否 (回滚/重试)
            │             │
            └─────────────┘
```

## 边界情况
1. **灾难性遗忘**：在针对某一类 Bad Case（如讽刺识别）优化时，可能会破坏模型在其他常见类别（如简单正面评价）上的表现，需每次迭代都运行全量回归测试。
2. **数据分布漂移**：随着时间推移，线上真实用户的提问方式可能发生变化（如出现新梗），导致旧的 Golden Dataset 失效，需定期注入最新的线上 Case 到评测集。
3. **模型版本升级**：当底层 LLM 升级（如从 GPT-4-Turbo 到 GPT-4O）时，原有的“最优 Prompt”可能不再是最优，甚至由于新模型对指令理解更敏感而产生过拟合，需重新评估。

## 面试追问
1. **过拟合问题**：如果 Prompt 在固定评测集上表现很好，但上线效果差怎么办？（评测集需定期更新，增加真实流量分布的数据，使用留出集验证）。
2. **CoT 触发**：什么时候引入 CoT（思维链）？（逻辑推理复杂任务，简单任务引入反而可能降低准确率并增加延迟）。
3. **评估指标**：如何评估主观任务（如创意写作）？（使用 LLM-as-a-Judge 进行打分，或设计基于细粒度维度的评分表）。

## 易错点
1. **盲目堆砌指令**：发现一个 Case 做错了，就直接在 Prompt 里加一句针对性指令，导致 Prompt 臃肿不堪、指令权重失衡，最终模型“不知道听谁的”。
2. **忽视温度参数**：在优化 Prompt 时未固定 Temperature 等生成参数，导致无法区分效果提升是源于 Prompt 改进还是源于模型采样的随机性。

## 记忆要点

- 迭代闭环：建评测集 -> 错误归因 -> 控制变量修改 -> 回归测试
- 错误分类：理解错、幻觉、格式错、合规问题，对症下药
- 数据驱动：记录版本改动与指标，避免凭直觉盲目调整
- 警惕灾难性遗忘：优化某类 Case 时需全量回归测试

## 结构化回答

**30 秒电梯演讲：** 迭代优化 Prompt 是数据驱动的闭环：建固定评测集（典型加边缘 Golden Dataset）→ 错误归因分类（理解错、幻觉、格式错、合规问题）→ 控制变量修改（一次改一个变量）→ 回归测试（全量跑防灾难性遗忘）。关键是记录每个版本改动和指标，避免凭直觉盲目调整。

**展开框架：**
1. **闭环四步** — 建评测集（量化评分标准）、错误归因（分类失败日志）、控制变量迭代（一次改指令或换 Few-shot）、数据驱动记录（版本改动加指标快照）。
2. **错误分类对症** — 理解错改指令措辞、知识幻觉加 RAG、格式错强化 Schema、合规问题加负面清单，不同错误不同药方。
3. **灾难性遗忘** — 优化某类 Case 可能破坏其他类别表现，每次迭代必须全量回归测试；评测集定期注入线上 Case 防分布漂移。

**收尾：** 我做电商情感分析时——讽刺识别率极低，分析 Bad Cases 发现模型把"真'好用'，一天充三次电"误判正面，加反语识别 Few-shot 后 F1 从 0.2 升到 0.85。您想深入聊过拟合评测集怎么办，还是 LLM-as-a-Judge 的主观任务评估？

## 视频脚本

> 预计时长：2 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：怎么迭代优化 Prompt | "像修车先确诊再换一个零件试跑，别同时换一堆。" | 类比开场 |
| 0:15 | 闭环四步流程图 | "建评测集、错误归因、控制变量修改、回归测试。" | 闭环框架 |
| 0:45 | 错误分类对症图 | "理解错改指令、幻觉加 RAG、格式错强 Schema、合规加负面清单。" | 错误分类 |
| 1:10 | 灾难性遗忘警示 | "坑：优化某类 Case 可能破坏其他类别，每次必须全量回归。" | 遗忘风险 |
| 1:35 | 讽刺识别案例 | "实战：反语误判正面，加 Few-shot 示例 F1 从 0.2 升到 0.85。" | 实战案例 |
| 1:50 | 总结口诀卡 | "记住：闭环四步，控制变量，全量回归防遗忘。下期讲 CoT。" | 收尾 |

