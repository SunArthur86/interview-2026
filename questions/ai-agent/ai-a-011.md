---
id: ai-a-011
difficulty: L2
category: ai-agent
subcategory: 工具使用
tags:
- String
- Integer
- IO
- Elasticsearch
feynman:
  essence: LLM不直接执行代码，而是输出结构化JSON指令让程序执行。
  analogy: 像厨子写菜谱让助手切菜，厨子不懂怎么切但知道要切多细。
  first_principle: 如何让语言模型以结构化方式安全可靠地操控外部逻辑？
  key_points:
  - LLM仅负责意图识别和参数生成，不直接运行逻辑
  - Schema设计重点：明确边界（何时用/不用）和参数含义
  - 工具粒度要适中，避免过于泛化或过于琐碎
  - 工具数量控制在5-15个，过多会导致选择困难
  - 返回的错误信息必须是LLM可理解的，便于自我修正
follow_up:
- 工具太多时如何做工具选择?
- 并行Function Calling如何实现?
memory_points:
- 流程：定义Schema -> LLM生成参数 -> 代码执行 -> 结果回传 -> LLM生成回复。
- 描述设计要明确边界（何时用/何时不），参数描述需包含期望值和格式。
- 工具粒度适中，数量控制在5-15个，避免LLM选择困难。
- 使用Enum类型减少幻觉，错误返回需包含可操作的修正建议。
---

# Function Calling的工作原理是什么?如何设计好的工具描述

- **Function Calling流程:**

1. 定义工具schema(name/description/parameters)
2. LLM根据用户意图选择工具并生成参数
3. 应用执行工具调用
4. 将结果返回给LLM
5. LLM基于结果生成最终回复

- **设计好的工具描述:**

```json
{
  "name": "search_docs",
  "description": "搜索内部文档库.当用户询问产品文档、API文档或技术规范时使用.不要用于搜索网页.",
  "parameters": {
    "query": {"type": "string", "description": "搜索关键词,英文优先"},
    "limit": {"type": "integer", "description": "返回结果数,默认5", "default": 5}
  }
}
```

- **最佳实践:**
1. **描述明确** - 说明何时用、何时不用
2. **参数语义** - 每个参数描述清楚期望值
3. **工具粒度** - 不太粗(一个工具做太多)也不太细(太多小工具)
4. **错误提示** - 工具返回错误时给LLM可操作的信息
5. **少即是多** - 工具数量控制在5-15个为佳

- **实战案例:**
在早期的股票查询Agent中，我们将工具描述写为"获取股票数据"，导致LLM频繁尝试查询不支持的实时分时数据。将描述改为"**仅支持获取A股日K线及财务指标**"后，无效请求减少了90%，不仅节省了Token，还避免了下游接口因不支持的高频查询而熔断。

- **代码示例 (Python):**
```python
import openai

tools = [{
    "type": "function",
    "function": {
        "name": "get_stock_price",
        "description": "获取A股日K线收盘价，不支持实时分时或美股数据",
        "parameters": {
            "type": "object",
            "properties": {
                "symbol": {"type": "string", "description": "股票代码，如 600519.SH"},
                "date": {"type": "string", "description": "日期，格式 YYYY-MM-DD"}
            },
            "required": ["symbol"]
        }
    }
}]
response = openai.chat.completions.create(model="gpt-4o", messages=[...], tools=tools)
```

- **Function Calling 内部流程图:**
```
User Query
    │
    ▼
┌─────────────────────────────────────┐
│  LLM Inference (System Prompt +     │
│  Tools Definitions in JSON Schema) │
└──────────────┬──────────────────────┘
               │
               │ 1. Decide to call tool
               ▼
┌─────────────────────────────────────┐
│  LLM Output (Special Token Mode)    │
│  { "name": "search", "arguments": {...}}│
└──────────────┬──────────────────────┘
               │
               │ 2. App parses JSON
               ▼
┌─────────────────────────────────────┐
│  Local Code Execution (Python/JS)   │
│  result = tool.search(args)         │
└──────────────┬──────────────────────┘
               │
               │ 3. Append result to history
               ▼
┌─────────────────────────────────────┐
│  LLM 2nd Pass (Synthesize Answer)   │
│  Context: Query + Tool Call + Result│
└──────────────┬──────────────────────┘
               │
               ▼
        Final Response
```

- **## 常见考点**
1. **参数定义规范**：为什么 `parameters` 必须定义为 JSON Schema 格式？`enum` 类型在定义中如何帮助 LLM 减少幻觉？
2. **Parallel Calling**：如何在一个 Response 中同时返回多个 `tool_calls`（例如 OpenAI 的格式），并处理并发执行？
3. **Null/None 处理**：当工具不需要参数时，Schema 应该定义为空对象 `{}` 还是省略该字段？错误的定义会导致什么问题？

## 核心知识点图

<img src="/interview-2026/images/diagram_ai-agent_ai-a-011.svg" alt="Function Calling的工作原理是什么?如何设计好的工具描述 - 核心知识点图" style="max-width:100%;height:auto;border:1px solid var(--border);border-radius:8px;margin:1em 0;" />

## 记忆要点

- 流程：定义Schema -> LLM生成参数 -> 代码执行 -> 结果回传 -> LLM生成回复。
- 描述设计要明确边界（何时用/何时不），参数描述需包含期望值和格式。
- 工具粒度适中，数量控制在5-15个，避免LLM选择困难。
- 使用Enum类型减少幻觉，错误返回需包含可操作的修正建议。

## 结构化回答

**30 秒电梯演讲：** Function Calling 就是让 LLM 输出结构化 JSON 指令，由程序去执行。LLM 只管意图识别和参数生成，不直接跑代码。好的工具描述要说清"何时用、何时不用"，工具数量控制在 5-15 个最舒服。

**展开框架：**
1. **五步流程** — 定义 Schema → LLM 生成参数 → 代码执行 → 结果回传 → LLM 生成回复，LLM 只负责意图和参数。
2. **描述设计** — 明确边界（何时用、何时不用），参数描述要包含期望值和格式，用 Enum 减少幻觉。
3. **工程实践** — 工具粒度适中，数量控制在 5-15 个；错误返回必须包含 LLM 可理解的修正建议。

**收尾：** 工具描述写得好不好，直接决定幻觉率——我可以讲个股票查询的例子，改一行描述省 90% 无效请求。

## 视频脚本

> 预计时长：2 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：Function Calling | "LLM 不直接跑代码，而是写 JSON 指令让程序执行，像厨子写菜谱。" | 核心机制 |
| 0:30 | 五步流程动画 | "五步：定义 Schema、生成参数、执行、回传、生成回复。" | 工作流程 |
| 1:10 | 好的工具描述示例 | "描述要说清何时用、何时不用，参数要标期望值和格式。" | 描述设计 |
| 1:40 | 股票查询案例对比 | "把'获取股票数据'改成'仅支持 A 股日 K 线'，无效请求少了九成。" | 实战价值 |

