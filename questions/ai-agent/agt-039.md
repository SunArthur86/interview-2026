---
id: agt-039
difficulty: L3
category: ai-agent
subcategory: 记忆系统
tags:
- Agent
feynman:
  essence: 区分「具体经历」与「通用知识」，采用不同的存储与衰减策略。
  analogy: 情景是日记（何时何地做了啥），语义是词典（定律与规则）。
  first_principle: 如何区分并管理「一次性的具体事件」与「通用的持久知识」？
  key_points:
  - 情景记忆：个人化、带时间、易过期。
  - 语义记忆：共享、稳定、规则性。
  - 区分存储可优化检索精度与隐私权限。
memory_points:
- 情景记忆：具体事件（时间/地点），带时间戳，高度个人化，随时间衰减。
- 语义记忆：一般知识/规则，全局共享，相对静态，更新频率低。
- 检索差异：情景偏时间邻近和隐私过滤；语义偏概念相似和全局索引。
- 实战避坑：物理隔离两者存储，情景记忆需加严格 ACL 防止隐私泄露。
- 存储建议：情景存 Vector DB（带 Timestamp）；语义存 Graph DB 或全局索引。
---

# 情景记忆与语义记忆

情景记忆与语义记忆源于认知心理学，在 Agent 中对应不同的存储与检索策略。

1.  **情景记忆**：关于具体事件（时间、地点、参与者）。例如「昨天下午用户要求改 PDF」。
    *   **实现**：带时间戳的对话片段、工具日志。
    *   **特点**：高度个人化，随时间衰减。
2.  **语义记忆**：关于一般性知识、规则。例如「公司退货政策是 7 天」。
    *   **实现**：知识库条目、FAQ、图谱三元组。
    *   **特点**：共享、稳定，更新频率低。

### 为什么要区分？
*   **检索特征**：情景检索偏时间邻近；语义检索偏概念相似。
*   **治理策略**：情景隐私级别高且易过期；语义需长期维护且多租户共享。

### 架构图：双层记忆架构
```text
┌─────────────────────────────────────────────────────────────┐
│                         Agent Core                           │
└───────────────────────────┬─────────────────────────────────┘
                            │
           ┌────────────────┼────────────────┐
           ▼                ▼                ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │   Working    │ │   Episodic   │ │   Semantic   │
    │   Memory     │ │   Memory     │ │   Memory     │
    │  (Context)   │ │ (Vector DB)  │ │ (Graph/KB)   │
    └──────────────┘ └──────┬───────┘ └──────┬───────┘
                            │                 │
                     [User Events]      [Domain Knowledge]
                     (Timestamped)      (Global/Rules)
```

### 实现策略
*   **情景记忆**：通常存储在向量数据库中，检索时强依赖时间衰减权重，防止过时信息干扰当前任务。
*   **语义记忆**：可以是向量库，也可以是知识图谱。对于规则类信息，KG 的结构化查询更精准；对于通用知识，向量库更灵活。

### 实战案例
*   **工程场景**：在构建 HR 助手时，用户问“我有多少年假？”。这属于**情景记忆**（需查该用户的个人余额）；而用户问“年假怎么请？”，这属于**语义记忆**（查公司制度）。混淆两者会导致通用错误回复或隐私泄露。架构设计时，必须将两者的 Collection 物理隔离，并对情景记忆增加严格的 ACL 校验。

### 对比表格：记忆类型差异
| 维度 | 情景记忆 | 语义记忆 |
| :--- | :--- | :--- |
| **数据来源** | 用户交互历史、Agent 行为日志 | 官方文档、手册、Wiki |
| **共享性** | 私有 | 全局共享 |
| **时效性** | 强依赖时间 (需要衰减) | 相对静态，仅在政策变更时更新 |
| **检索逻辑** | "用户最近做了什么" | "通用规则是什么" |
| **存储建议** | Vector DB (带 Timestamp) | Graph DB / Vector DB (全局索引) |

### 代码模型（Python）
```python
@dataclass
class MemoryObject:
    type: str # "episodic" or "semantic"
    content: str
    metadata: dict
    
    def get_search_filter(self):
        if self.type == "episodic":
            # 情景记忆：强过滤，必须匹配用户，且通常是近期
            return {"must": [{"key": "user_id", "match": {"value": self.metadata["user_id"]}}]}
        else:
            # 语义记忆：无用户过滤，仅匹配业务领域
            return {"must": [{"key": "domain", "match": {"value": self.metadata["domain"]}}]}
```


## 核心流程图

```mermaid
flowchart TD
    Start([🚀 SQL 请求到达]):::start
    Parser[解析器 Parser<br/>词法/语法分析]:::process
    AST[生成抽象语法树 AST]:::process
    Preproc[预处理器<br/>语义检查 + 权限]:::process
    Optimizer[优化器 Optimizer]:::process
    CostQ{{基于代价选择?<br/>CBO}}:::decision
    IdxScan[索引扫描<br/>range/ref]:::process
    FullScan[全表扫描<br/>ALL]:::warn
    Execute[执行器 Executor<br/>调用存储引擎接口]:::process
    EngineQ{{存储引擎?<br/>InnoDB/MyISAM}}:::decision
    InnoDB[InnoDB 引擎]:::process
    BufferPool[Buffer Pool<br/>内存缓冲池]:::store
    HitQ{{页命中 Buffer Pool?}}:::decision
    ReadDisk[从磁盘读取页<br/>随机 IO]:::warn
    RedoLog[(redo log<br/>WAL 先写日志)]:::store
    BinLog[(binlog<br/>主从复制)]:::store
    UndoLog[(undo log<br/>事务回滚/MVCC)]:::store
    CommitQ{{是否提交事务?<br/>2PC}}:::decision
    TwoPhase[Prepare → 写 redo<br/>→ 写 binlog → Commit]:::process
    Crash[宕机崩溃恢复<br/>redo 重放 + binlog 校验]:::danger
    Final([✅ 返回结果集]):::start

    Start --> Parser --> AST --> Preproc --> Optimizer
    Optimizer --> CostQ
    CostQ -->|有合适索引| IdxScan --> Execute
    CostQ -->|无索引/全表| FullScan --> Execute
    Execute --> EngineQ
    EngineQ -->|默认| InnoDB --> BufferPool
    EngineQ -->|旧版| FullScan
    BufferPool --> HitQ
    HitQ -->|命中| Execute
    HitQ -->|未命中| ReadDisk --> BufferPool
    InnoDB -.修改.-> UndoLog
    InnoDB -.修改.-> RedoLog
    InnoDB -.提交.-> BinLog
    Execute --> CommitQ
    CommitQ -->|是| TwoPhase --> Final
    CommitQ -->|崩溃| Crash --> RedoLog

    classDef start fill:#2563eb,stroke:#1e3a8a,color:#fff,stroke-width:2px;
    classDef process fill:#dbeafe,stroke:#3b82f6,color:#1e3a8a;
    classDef decision fill:#fef3c7,stroke:#f59e0b,color:#78350f,stroke-width:2px;
    classDef store fill:#8b5cf6,stroke:#6d28d9,color:#fff;
    classDef warn fill:#fee2e2,stroke:#ef4444,color:#7f1d1d;
    classDef danger fill:#b91c1c,stroke:#7f1d1d,color:#fff,stroke-width:2px;
```

## 记忆要点

- 情景记忆：具体事件（时间/地点），带时间戳，高度个人化，随时间衰减。
- 语义记忆：一般知识/规则，全局共享，相对静态，更新频率低。
- 检索差异：情景偏时间邻近和隐私过滤；语义偏概念相似和全局索引。
- 实战避坑：物理隔离两者存储，情景记忆需加严格 ACL 防止隐私泄露。
- 存储建议：情景存 Vector DB（带 Timestamp）；语义存 Graph DB 或全局索引。

## 结构化回答

**30 秒电梯演讲：** 情景记忆是"日记"（何时何地做了啥，个人化易过期），语义记忆是"词典"（定律规则，全局共享稳定）。区分存储能优化检索精度和隐私权限——情景要带时间戳衰减、严格 ACL，语义要长期维护、多租户共享。混淆两者会出大问题。

**展开框架：**
1. **本质区别** — 情景是具体事件（用户昨天改了 PDF），带时间戳高度个人化；语义是通用知识（公司退货政策 7 天），全局共享相对静态。
2. **检索逻辑不同** — 情景偏时间邻近 + 隐私过滤（user_id 强制匹配）；语义偏概念相似 + 全局索引（按业务领域）。
3. **物理隔离是必须** — 两者 Collection 要物理分开，情景记忆加严格 ACL 防隐私泄露，语义记忆可多租户共享。
4. **存储介质建议** — 情景存 Vector DB 带 Timestamp；语义存 Graph DB（规则精准）或全局向量索引（灵活）。

**收尾：** 我做过 HR 助手，"我有多少年假"（情景，查个人余额）和"年假怎么请"（语义，查制度）必须分库，混了要么通用错误回复要么隐私泄露。您想深入聊存储隔离、ACL 设计还是衰减策略？

## 视频脚本

> 预计时长：3 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：情景 vs 语义记忆 | "日记和词典要分开存，这就是情景记忆和语义记忆的区别。" | 开场钩子 |
| 0:25 | 日记 vs 词典类比 | "情景是日记，何时何地做了啥，个人化易过期；语义是词典，定律规则，全局共享稳定。" | 本质区别 |
| 0:55 | 双层记忆架构图 | "情景存 Vector DB 带时间戳衰减；语义存 Graph DB 或全局索引。检索逻辑完全不同。" | 存储与检索 |
| 1:35 | 检索差异对比表 | "情景偏时间邻近和隐私过滤，必须匹配 user_id；语义偏概念相似和全局索引，按业务领域。" | 检索差异 |
| 2:10 | HR 助手年假案例 | "实战：HR 助手'我有多少年假'是情景查个人余额，'年假怎么请'是语义查制度。必须分库物理隔离。" | 实战案例 |
| 2:45 | 总结卡 | "记住：日记词典分库、情景加 ACL、语义可共享。下期讲记忆检索策略。" | 收尾 |

