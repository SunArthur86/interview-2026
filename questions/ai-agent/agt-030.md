---
id: agt-030
difficulty: L3
category: ai-agent
subcategory: 工具调用
tags:
- Agent
feynman:
  essence: 缩小工具候选集，提升决策效率。
  analogy: 先去图书馆目录室查分类，再去书架找具体的书。
  first_principle: 如何在海量工具中快速找到最相关的少数几个？
  key_points:
  - 工具量过大（>50）时必须考虑路由
  - 描述质量直接决定向量路由效果
  - 可混合规则和向量检索提升准确率
  - 路由本身也有成本，需权衡
memory_points:
- 背景：工具过多导致Token耗尽、注意力分散，路由用于缩小候选集。
- 向量路由：向量化工具描述，Query检索Top-K，速度快语义好。
- 分类路由：训练轻量分类器（BERT）预测类别，准确率高但需数据。
- 层级路由：先选组再选工具，适合结构化明显的工具集。
- 优化策略：Top-K设5-10个，混合BM25防漏选，工具描述需含场景示例。
---

# 工具路由（Tool Routing）

### 工具路由

**背景**：当工具数量过多（几十至上百个）时，全量发给模型会导致 Token 耗费高、上下文窗口溢出、模型注意力分散导致误选率高（“迷失在中间”现象）。路由用于缩小候选集。

**策略**：
1.  **向量检索**：将工具描述 embedding，Query 检索 Top-K 工具。速度快，语义匹配好。
2.  **分类模型**：训练轻量分类器（BERT/小 LLM）直接预测工具类别。准确率高，但需训练数据。
3.  **层级目录**：先选组（如 `db_group`），再选具体工具。适合结构化明显的工具集。
4.  **语义路由**：基于规则或 LLM 判断意图类型，映射到不同的子 Agent 或工具集。

**路由流程图**：
```text
User Query
   │
   ▼
┌──────────────┐
│  Router Layer│ (Embedding or Classifier)
└──────┬───────┘
       │
       │ Select Top-K Candidates
       │ (e.g., Weather_Tool, News_Tool)
       ▼
┌──────────────┐
│ LLM + Tools  │ (Only K tools in context)
└──────────────┘
```

**实战案例**：
某 SaaS 平台集成 200+ ERP API。直接将所有 API 传给 GPT-4 导致费用极高且常选错接口。引入语义路由层后，先用 BGE-M3 向量模型筛选 Top 5 相关 API，再发给 LLM 最终决策。API 调用准确率维持在 95% 的同时，Prompt Token 消耗减少了 80%。

**代码示例（向量路由）**：
```python
import numpy as np
from openai import OpenAI

def route_tools(query: str, tools: list, client: OpenAI, top_k: int = 3):
    # 1. 向量化工具描述
    texts = [f"{t.name}\n{t.description}" for t in tools]
    doc_emb = client.embeddings.create(input=texts, model="text-embedding-3-small")
    doc_vecs = [d.embedding for d in doc_emb.data]
    
    # 2. 向量化 Query
    q_emb = client.embeddings.create(input=[query], model="text-embedding-3-small")
    q_vec = q_emb.data[0].embedding
    
    # 3. 计算余弦相似度并返回 Top-K
    scores = np.dot(doc_vecs, q_vec)
    top_indices = np.argsort(-scores)[:top_k]
    return [tools[i] for i in top_indices]
```

**面试金句**：
*   **Q：向量路由选不对怎么办？**
*   **A**：增大 Top-K、混合关键词检索（BM25）、加一层 LLM 二分类、保留通用搜索兜底（Re-Rank 机制）。

## 常见考点
1.  **召回率与准确率权衡**：Top-K 设为多少合适？如何平衡“漏掉工具”和“给模型太多干扰”？（通常根据经验值，如 5-10 个，并监控未匹配率）。
2.  **工具描述优化**：为了提升向量检索效果，工具描述应该怎么写？（包含具体的使用场景示例、同义词，而非简单的功能定义）。

**对比表格：路由策略对比**

| 策略 | 向量检索 | LLM 分类器 | 层级目录 |
| :--- | :--- | :--- | :--- |
| **实现成本** | 低 (无训练) | 中 (需标注数据) | 中 (需定义结构) |
| **运行延迟** | 极低 (<50ms) | 中 (推理耗时) | 低 (匹配规则) |
| **语义理解** | 强 | 最强 | 弱 (依赖字面匹配) |
| **动态扩展** | 好 (自动更新索引) | 差 (需重训模型) | 差 (需手动维护树) |
| **适用场景** | 通用场景，工具数量多 | 复杂意图，高精度要求 | 工具分类清晰 (如按部门) |


## 核心流程图

```mermaid
flowchart TD
    Start([🚀 SQL 请求到达]):::start
    Parser[解析器 Parser<br/>词法/语法分析]:::process
    AST[生成抽象语法树 AST]:::process
    Preproc[预处理器<br/>语义检查 + 权限]:::process
    Optimizer[优化器 Optimizer]:::process
    CostQ{{基于代价选择?<br/>CBO}}:::decision
    IdxScan[索引扫描<br/>range/ref]:::process
    FullScan[全表扫描<br/>ALL]:::warn
    Execute[执行器 Executor<br/>调用存储引擎接口]:::process
    EngineQ{{存储引擎?<br/>InnoDB/MyISAM}}:::decision
    InnoDB[InnoDB 引擎]:::process
    BufferPool[Buffer Pool<br/>内存缓冲池]:::store
    HitQ{{页命中 Buffer Pool?}}:::decision
    ReadDisk[从磁盘读取页<br/>随机 IO]:::warn
    RedoLog[(redo log<br/>WAL 先写日志)]:::store
    BinLog[(binlog<br/>主从复制)]:::store
    UndoLog[(undo log<br/>事务回滚/MVCC)]:::store
    CommitQ{{是否提交事务?<br/>2PC}}:::decision
    TwoPhase[Prepare → 写 redo<br/>→ 写 binlog → Commit]:::process
    Crash[宕机崩溃恢复<br/>redo 重放 + binlog 校验]:::danger
    Final([✅ 返回结果集]):::start

    Start --> Parser --> AST --> Preproc --> Optimizer
    Optimizer --> CostQ
    CostQ -->|有合适索引| IdxScan --> Execute
    CostQ -->|无索引/全表| FullScan --> Execute
    Execute --> EngineQ
    EngineQ -->|默认| InnoDB --> BufferPool
    EngineQ -->|旧版| FullScan
    BufferPool --> HitQ
    HitQ -->|命中| Execute
    HitQ -->|未命中| ReadDisk --> BufferPool
    InnoDB -.修改.-> UndoLog
    InnoDB -.修改.-> RedoLog
    InnoDB -.提交.-> BinLog
    Execute --> CommitQ
    CommitQ -->|是| TwoPhase --> Final
    CommitQ -->|崩溃| Crash --> RedoLog

    classDef start fill:#2563eb,stroke:#1e3a8a,color:#fff,stroke-width:2px;
    classDef process fill:#dbeafe,stroke:#3b82f6,color:#1e3a8a;
    classDef decision fill:#fef3c7,stroke:#f59e0b,color:#78350f,stroke-width:2px;
    classDef store fill:#8b5cf6,stroke:#6d28d9,color:#fff;
    classDef warn fill:#fee2e2,stroke:#ef4444,color:#7f1d1d;
    classDef danger fill:#b91c1c,stroke:#7f1d1d,color:#fff,stroke-width:2px;
```

## 记忆要点

- 背景：工具过多导致Token耗尽、注意力分散，路由用于缩小候选集。
- 向量路由：向量化工具描述，Query检索Top-K，速度快语义好。
- 分类路由：训练轻量分类器（BERT）预测类别，准确率高但需数据。
- 层级路由：先选组再选工具，适合结构化明显的工具集。
- 优化策略：Top-K设5-10个，混合BM25防漏选，工具描述需含场景示例。

## 结构化回答

**30 秒电梯演讲：** 工具路由就是"先查目录室再上书架"——当工具超过 50 个时，全量发给模型会 Token 爆炸还会"迷失在中间"选错。用向量检索先把工具描述 embedding，Query 检索出 Top 5-10 个候选再交给 LLM 决策，Token 砍 80% 准确率还不掉。

**展开框架：**
1. **为什么需要路由** — 工具太多导致 Token 耗尽、上下文溢出、注意力分散误选；路由先缩小候选集再交给 LLM。
2. **三种策略** — 向量检索（描述 embedding 查 Top-K，快）、分类模型（BERT 预测类别，准但要数据）、层级目录（先选组再选工具，适合结构化）。
3. **参数与优化** — Top-K 设 5-10 个平衡漏选和干扰；混合 BM25 防漏选；工具描述要含场景示例和同义词。
4. **路由本身有成本** — 别为了路由而路由，工具少（<20）直接全量给模型可能更简单。

**收尾：** 我做过 200+ ERP API 的 SaaS，全量发给 GPT-4 费用高还选错，上 BGE-M3 向量路由筛 Top 5 后 Token 降 80%、准确率维持 95%。您想深入聊向量路由、分类模型还是层级目录？

## 视频脚本

> 预计时长：3 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：工具路由 | "工具超过 50 个全量发模型会 Token 爆炸，先路由缩小候选集。" | 开场钩子 |
| 0:25 | 图书馆目录室类比 | "像去图书馆，先查目录室找分类，再去书架找具体的书，别一次把所有书搬出来。" | 本质类比 |
| 1:00 | 向量/分类/层级三策略 | "三种策略：向量检索快、分类模型准但要数据、层级目录先选组再选工具。" | 路由策略 |
| 1:40 | Top-K 5-10 + 混合 BM25 | "Top-K 设 5-10 个，混合 BM25 防漏选，工具描述要含场景示例和同义词。" | 优化策略 |
| 2:15 | 200+ ERP API Token 降 80% 案例 | "实战：200+ ERP API 用 BGE-M3 向量路由筛 Top 5，Token 降 80%、准确率维持 95%。" | 实战案例 |
| 2:50 | 总结卡 | "记住：工具多才路由、向量首选、Top-K 平衡。下期讲工具编排。" | 收尾 |

