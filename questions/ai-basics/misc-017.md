---
id: misc-017
difficulty: L2
category: ai-basics
subcategory: 训练与微调
tags:
- IOC
feynman:
  essence: 利用模型生成数据解决稀缺问题，但需防范质量退化与偏见。
  analogy: 就像学生抄作业抄多了，知识点没掌握，只会抄错的。
  first_principle: 如何在缺乏高质量数据时，低成本扩展数据集且不损害模型能力？
  key_points:
  - 弥补特定领域数据缺口
  - 最大风险是模型坍缩
  - 需混合真实数据保底
  - 必须进行严格质量过滤
follow_up:
- 什么是Model Collapse?如何检测?
- 如何评估合成数据的质量?
memory_points:
- 使用场景：领域数据不足、SFT指令生成、偏好对构造
- 三大风险：模型坍缩（多样性减）、偏见放大、幻觉传播
- 缓解措施：混合30%以上真实数据、质量过滤、多样性去重
- 误区：低质量合成数据是毒药，会导致灾难性遗忘
---

# 大模型训练中为什么大量使用合成数据?合成数据有什么风险

- **合成数据使用场景:**
1. **领域数据不足** - 医疗/法律/低资源语言
2. **SFT数据生成** - 用强模型生成指令-回复对
3. **偏好数据生成** - 自动生成排序对
4. **推理数据** - 自动生成CoT推理路径

- **主要方法:**
- Self-Instruct: 用强模型自动生成训练数据
- Evol-Instruct: 递增复杂度的指令生成

- **风险:**
1. **模型坍缩** - 用模型生成数据训练下一代模型,多样性递减
2. **偏见放大** - 生成数据的偏差被放大
3. **幻觉传播** - 模型幻觉产生的错误数据被训练

- **缓解措施:**
- 混合真实数据(至少30%)
- 质量过滤
- 多样性约束(embedding聚类去重)

- **边界情况补充:**
  - **长尾领域**: 对于极度缺乏数据的垂直领域（如某种稀有病的诊断），合成数据中若有事实性错误，模型会"死记硬背"这些错误，且比在通用领域更难修正。
  - **评估数据污染**: 使用合成数据训练时，严禁将合成数据混入测试集，否则会造成高分假象，需严格进行数据去重。
  - **格式一致性**: 合成数据（特别是JSON等结构化数据）若格式约束不严，会导致训练出的模型在输出格式上非常松散。

- **实战案例:**
某金融问答项目中，使用GPT-4生成10万条财报问答对进行微调，结果模型在回答长尾问题时频繁出现“编造财报数据”的幻觉。引入基于Self-CheckGPT的过滤机制后，幻觉率下降了40%。

- **代码示例 (Quality Filter):**
```python
# 使用Self-CheckGPT思想过滤合成数据中的幻觉样本
def filter_hallucination(samples, teacher_model, threshold=0.5):
    clean_data = []
    for item in samples:
        # 生成多个样本的一致性检查
        others = teacher_model.generate(item['prompt'], n=3)
        # 计算当前答案与其他生成答案的相似度(BERTScore)
        score = calculate_similarity(item['response'], others)
        if score > threshold:  # 一致性高，保留
            clean_data.append(item)
    return clean_data
```

- **## 面试追问:**
1. 合成数据的多样性（Diversity）是如何量化的？如果检测到多样性下降，在数据生成阶段应如何调整采样策略？
2. 在多轮对话数据合成中，如何保证上下文的一致性？避免模型在第一轮回答了A，第三轮却回答B的矛盾情况？
3. 如何区分"良性合成数据"（如格式转换）和"恶性合成数据"（如虚构事实）？针对两者应采取不同的训练权重吗？

- **## 易错点:**
1. **误区：合成数据越多越好**。低质量合成数据是"毒药"，会通过Catastrophic Forgetting机制覆盖原始知识。
2. **误区：只过滤难度**。在合成数据中，相比过滤"太简单"的样本，过滤"错误但看起来自信"的样本更为重要且更困难。

- **## 常见考点:**
1. 在实践中如何过滤低质量的合成数据？
2. 什么是「模型自食其子」现象？
3. 合成数据在代码和数学领域的有效性为什么比在通用聊天领域更高？

## 记忆要点

- 使用场景：领域数据不足、SFT指令生成、偏好对构造
- 三大风险：模型坍缩（多样性减）、偏见放大、幻觉传播
- 缓解措施：混合30%以上真实数据、质量过滤、多样性去重
- 误区：低质量合成数据是毒药，会导致灾难性遗忘

## 结构化回答

**30 秒电梯演讲：** 合成数据是用模型自己生成数据来解决数据稀缺，常见场景是领域数据不足、SFT 指令生成、偏好对构造。它的最大风险是模型坍缩，就像学生抄作业抄多了，只会抄错的。缓解办法是必须掺 30% 以上真实数据保底，再做严格的质量过滤和多样性去重。

**展开框架：**
1. **使用场景** — 领域数据不足（如医疗、法律垂直域）、SFT 指令和答案生成、构造偏好对（Chosen/Rejected），是低成本扩展数据集的关键手段。
2. **三大风险** — 模型坍缩（多样性下降，代际退化）、偏见放大（错误被循环强化）、幻觉传播（编造内容被当成事实再喂回去）。
3. **缓解措施** — 混合 30% 以上真实数据、严格质量过滤、多样性去重；低质量合成数据是毒药，会导致灾难性遗忘。

**收尾：** 一句话，合成数据是双刃剑，用得好是飞轮，用不好是毒药。您想深入聊聊什么是 Model Collapse 以及怎么检测，还是怎么评估合成数据质量？

## 视频脚本

> 预计时长：2 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题《合成数据：飞轮还是毒药》+ 学生抄作业漫画 | 合成数据就像学生抄作业，抄多了知识点没掌握，只会抄错的。用得好是飞轮，用不好是毒药。 | 类比开场 |
| 0:25 | 三大场景图标：领域不足/SFT指令/偏好对 | 它的典型场景有三个：领域数据不足、SFT 指令生成、偏好对构造，都是低成本扩数据的路子。 | 使用场景 |
| 0:55 | 三大风险图：模型坍缩/偏见放大/幻觉传播 | 但有三大风险：模型坍缩导致多样性下降，偏见被循环放大，幻觉被当成事实传播。 | 三大风险 |
| 1:25 | 缓解措施三件套：30%真实数据 + 质量过滤 + 多样性去重 | 缓解办法很明确：必须掺 30% 以上的真实数据保底，再做严格的质量过滤和多样性去重。 | 缓解措施 |
| 1:50 | 红色警告：低质量合成 = 灾难性遗忘 | 最后一条铁律：低质量合成数据是毒药，会导致灾难性遗忘，宁缺毋滥。 | 风控红线 |

