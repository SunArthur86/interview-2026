---
id: dsl-054
difficulty: L5
category: java-core
subcategory: Java基础
feynman:
  essence: Flexbox 是一种一维布局模型，通过主轴和交叉轴控制元素对齐与分配空间。
  analogy: 像整理行李箱，既能把衣服整齐排列（对齐），也能挤压拉伸填满空隙（伸缩）。
  first_principle: 如何让容器内的元素自动适应容器大小变化并实现对齐？
  key_points:
  - '容器设置display: flex开启弹性布局'
  - 主轴方向（flex-direction）决定排列顺序
  - justify-content控制主轴对齐，align-items控制交叉轴对齐
  - flex属性控制子项的伸缩比例
memory_points:
- 核心轴：父级定义主轴方向与换行，justify管主轴对齐，align管交叉轴对齐。
- 子项缩放：grow管放大分配剩余空间，shrink管缩小填补溢出空间，basis定基础尺寸。
- flex简写：flex:1 等同于 1 1 0%（自动占满剩余），flex:none 等同于 0 0 auto（不伸缩）。
- 实战排坑：因子元素float失效，故全用flex；定高容器内设flex:1可免算高自动撑满剩余区域。
---

# flex布局

### Flex 弹性布局

弹性布局旨在为盒模型提供最大的灵活性，能够简便、完整、响应式地实现各种页面布局。布局由父容器和子容器构成，通过主轴和交叉轴控制子容器排列。注意：设为 Flex 布局后，子元素的 `float`、`clear` 和 `vertical-align` 属性将失效。

#### 架构图

```text
      Main Axis (主轴) →
    ┌─────────────────────┐
    │  ┌───┐ ┌───┐ ┌───┐  │
    │  │ A │ │ B │ │ C │  │  Cross Axis
    │  └───┘ └───┘ └───┘  │  (交叉轴) ↓
    └─────────────────────┘
         Flex Container
```

#### 1. 父容器属性

- **flex-direction**: 决定主轴的方向。
  - `row` (默认): 主轴为水平方向，起点在左端。
  - `row-reverse`: 主轴为水平方向，起点在右端。
  - `column`: 主轴为垂直方向，起点在上沿。
  - `column-reverse`: 主轴为垂直方向，起点在下沿。

- **flex-wrap**: 决定容器内项目是否换行。
  - `nowrap` (默认): 不换行。当主轴排不下时，会根据子元素的 `flex-shrink` 比例进行压缩，而非直接溢出。
  - `wrap`: 换行，第一行在上方。
  - `wrap-reverse`: 换行，第一行在下方。

- **flex-flow**: `flex-direction` 和 `flex-wrap` 的简写，默认值为 `row nowrap`。

- **justify-content**: 定义项目在主轴上的对齐方式。
  - `flex-start` (默认): 左对齐。
  - `flex-end`: 右对齐。
  - `center`: 居中。
  - `space-between`: 两端对齐，项目之间的间隔都相等。
  - `space-around`: 每个项目两侧的间隔相等。所以，项目之间的间隔比项目与边框的间隔大一倍。
  - `space-evenly`: 每个项目之间的间隔以及项目与边框的间隔都相等（部分浏览器支持需注意兼容性）。

- **align-items**: 定义项目在交叉轴上如何对齐。
  - `stretch` (默认): 如果项目未设置高度或设为 auto，将占满整个容器的高度。
  - `flex-start`: 交叉轴的起点对齐。
  - `flex-end`: 交叉轴的终点对齐。
  - `center`: 交叉轴的中点对齐。
  - `baseline`: 项目的第一行文字的基线对齐。

- **align-content**: 定义了多根轴线的对齐方式。如果项目只有一根轴线，该属性不起作用。
  - `flex-start`: 与交叉轴的起点对齐。
  - `flex-end`: 与交叉轴的终点对齐。
  - `center`: 与交叉轴的中点对齐。
  - `space-between`: 与交叉轴两端对齐，轴线之间的间隔平均分布。
  - `space-around`: 每根轴线两侧的间隔都相等。
  - `stretch` (默认): 轴线占满整个交叉轴。

#### 2. 子容器属性

- **order**: 定义项目的排列顺序。数值越小，排列越靠前，默认为 0。

- **flex-grow**: 定义项目的放大比例。
  - 默认为 `0`，即如果存在剩余空间，也不放大。
  - 如果所有项目的 `flex-grow` 属性都为 1，则它们将等分剩余空间。
  - 如果一个项目的 `flex-grow` 属性为 2，其他项目都为 1，则前者占据的剩余空间将比其他项多一倍。
  - **关键细节**：如果所有项目的 `flex-grow` 之和小于 1，则剩余空间按比例分配，但不会全部分配完（即只会分配总和比例的部分剩余空间）。

- **flex-shrink**: 定义了项目的缩小比例。
  - 默认为 `1`，即如果空间不足，该项目将缩小。
  - 如果所有项目的 `flex-shrink` 属性都为 1，当空间不足时，都将等比例缩小。
  - 如果一个项目的 `flex-shrink` 属性为 0，其他项目都为 1，则当空间不足时，前者不缩小。
  - **负值**：对该属性无效。

- **flex-basis**: 定义了在分配多余空间之

**实战案例**：在实现“固定头部、可滚动内容区、底部固定按钮”的移动端布局时，常遇到内容区高度计算不准确的问题。使用 Flex 布局设置 `flex-direction: column` 和 `flex: 1` (即 `flex-grow: 1; flex-shrink: 1; flex-basis: 0%`) 可自动占据剩余空间，完美解决无需计算具体高度的响应式难题。

**代码示例**：
```css
/* 经典的圣杯布局/固定头部底部布局 */
.container {
  display: flex;
  flex-direction: column;
  height: 100vh;
}
.header, .footer {
  flex: 0 0 auto; /* 不放大不缩小，基于内容高度 */
}
.content {
  flex: 1; /* 等同于 flex: 1 1 0%，占据剩余空间，优先级高 */
  overflow-y: auto; /* 内容溢出滚动 */
}
```

## 核心知识点图

<img src="/interview-2026/images/diagram_java-core_dsl-054.svg" alt="核心知识点图" style="max-width:100%;height:auto;border:1px solid var(--border);border-radius:8px;margin:1em 0;" />

## 记忆要点

- 核心轴：父级定义主轴方向与换行，justify管主轴对齐，align管交叉轴对齐。
- 子项缩放：grow管放大分配剩余空间，shrink管缩小填补溢出空间，basis定基础尺寸。
- flex简写：flex:1 等同于 1 1 0%（自动占满剩余），flex:none 等同于 0 0 auto（不伸缩）。
- 实战排坑：因子元素float失效，故全用flex；定高容器内设flex:1可免算高自动撑满剩余区域。

## 结构化回答

**30 秒电梯演讲：** Flexbox 是一种一维布局模型，通过主轴和交叉轴控制元素对齐与分配空间。打个比方，像整理行李箱，既能把衣服整齐排列（对齐），也能挤压拉伸填满空隙（伸缩）。

**展开框架：**
1. **核心轴** — 父级定义主轴方向与换行，justify管主轴对齐，align管交叉轴对齐。
2. **子项缩放** — grow管放大分配剩余空间，shrink管缩小填补溢出空间，basis定基础尺寸。
3. **flex简写** — flex:1 等同于 1 1 0%（自动占满剩余），flex:none 等同于 0 0 auto（不伸缩）。

**收尾：** 我在项目里踩过坑——在实现“固定头部、可滚动内容区、底部固定按钮”的移动端布局时，常遇到内容区高度计算不准确的问题。您想深入聊哪一段：原理、避坑还是对比选型？

## 视频脚本

> 预计时长：4 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：flex布局 | "flex布局？一句话——像整理行李箱，既能把衣服整齐排列（对齐），也能挤压拉伸填满空隙（伸缩）。" | 开场钩子 |
| 0:48 | 概念动画/示意图 | "Flexbox 是一种一维布局模型，通过主轴和交叉轴控制元素对齐与分配空间——像整理行李箱，既能把衣服整齐排列（对齐），也能挤压拉伸填满空隙（伸缩）" | 核心定义 |
| 1:36 | 核心轴示意 | "父级定义主轴方向与换行，justify管主轴对齐，align管交叉轴对齐。" | 要点1 |
| 2:24 | 子项缩放示意 | "grow管放大分配剩余空间，shrink管缩小填补溢出空间，basis定基础尺寸。" | 要点2 |
| 3:12 | flex简写示意 | "flex:1 等同于 1 1 0%（自动占满剩余），flex:none 等同于 0 0 auto（不伸缩）。" | 要点3 |
| 4:00 | 总结卡 | "记住这几条，面试不慌。下期讲进阶追问。" | 收尾 |
