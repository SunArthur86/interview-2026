---
id: jnf-008
difficulty: L2
category: java-core
subcategory: JDK新特性
feynman:
  essence: 统一所有集合访问首尾元素和逆序的API。
  analogy: 像给所有集装箱装上通用的“首尾门”和“倒车开关”，不管箱子类型。
  first_principle: 如何在集合框架中统一“有序访问”的操作规范？
  key_points:
  - 定义了SequencedCollection、SequencedSet、SequencedMap三大接口
  - 统一了getFirst/addFirst/reversed等操作方法
  - List、Deque、LinkedHashSet等旧类均实现了该接口
  - 解决了过去不同集合访问首尾方式不一致的痛点
  - 引入SequencedCollection/Map接口
  - 统一getFirst/getLast/addFirst等操作
  - reversed()返回反向视图
  - List、LinkedHashSet、TreeMap等均已实现
memory_points:
- 核心痛点：Java 21前获取末尾元素API割裂，如List需算size减一，Map更是无直接方法。
- 三大统一接口：新增SequencedCollection, SequencedSet, SequencedMap。
- 核心方法：提供统一的getFirst()、addLast()及逆序遍历reversed()方法。
- 实战应用：为LinkedHashMap直接补充pollLastEntry()，让LRU淘汰逻辑从繁杂迭代变为一行代码。
- 痛点：解决List、Deque、LinkedHashSet等有序集合首尾操作API各不相同的问题
- 核心：新增SequencedCollection统一接口，提供getFirst、addLast等方法
- reversed()：返回双向迭代视图而非新集合，修改视图直接影响原对象
- 避坑：ArrayList调用addFirst时间复杂度为O(N)，大数量批量操作慎用
follow_up: []
tags: []
---

# 什么是Sequenced Collections（顺序集合）？Java 21引入了哪些新接口和方法？

Sequenced Collections是Java 21（JEP 431）引入的一组新接口，为Java现有集合层次结构补充了一致的顺序访问API。在Java 21之前，Java集合接口在访问有序集合的首尾元素时有不一致且令人沮丧的API：List使用get(0)和get(list.size()-1)，Deque使用getFirst()和getLast()，LinkedHashSet根本没有直接的首尾访问方法。

Java 21引入了三个新接口来统一这些操作：SequencedCollection, SequencedSet, SequencedMap。

```java
// SequencedCollection - 新接口
public interface SequencedCollection<E> extends Collection<E> {
    E getFirst();          // 获取第一个元素
    E getLast();           // 获取最后一个元素
    void addFirst(E e);    // 添加到头部
    void addLast(E e);     // 
}
```

这些接口被追溯地添加到现有集合类上——List、Deque、LinkedHashSet、LinkedHashMap、SortedSet、SortedMap及其实现类——使得新方法在所有现有Java代码中立即可用。

#### 实战案例
在实现“最近浏览记录”功能时，通常使用LinkedHashMap保持插入顺序并淘汰最旧数据。以前必须通过复杂的迭代器或获取entrySet来判断“首尾”，现在可以直接使用`map.removeFirst()`和`map.putLast()`，代码逻辑从“实现细节导向”转变为“业务语义导向”。

#### 实战代码：LRU Cache 简化实现

```java
// 实战代码：利用 SequencedMap 快速实现 LRU 淘汰逻辑
public class SimpleLRU<K, V> extends LinkedHashMap<K, V> {
    private final int maxSize;

    public SimpleLRU(int maxSize) {
        this.maxSize = maxSize;
    }

    @Override
    protected boolean removeEldestEntry(Map.Entry<K, V> eldest) {
        // 使用 size() 直观判断，无需额外维护计数器
        return size() > maxSize; 
    }
    
    // 实战：获取最旧数据（尾部）
    public V getOldest() {
        return this.lastEntry().getValue();
    }
}
```

#### 对比表格：Sequenced API vs 传统 API

| 操作 | 传统 API | Sequenced API (Java 21) |
| :--- | :--- | :--- |
| **获取第一个** | `list.get(0)` | `list.getFirst()` (空指针友好) |
| **获取最后一个** | `list.get(list.size()-1)` | `list.getLast()` |
| **添加到头部** | `list.add(0, elem)` | `list.addFirst(elem)` |
| **逆序遍历** | `for (int i=size-1; i>=0; i--)` | `for (var e : list.reversed())` |
| **适用集合** | List, Deque 等 (不统一) | List, Set, Map (统一接口) |

### 架构图：集合继承层次结构增强

```text
          Iterable
               ↑
        Collection
               ↑
    ┌──────────┴──────────┐
    │                     │
    List          SequencedCollection  <-- 新增接口
    │                     ↑
    │              ┌──────┴──────┐
    │              │             │
    │           Deque    LinkedHashSet (Modifies)
    │              │             │
    │              └──────┬──────┘
    │                     │
    └─────────┬───────────┘
              │
        SequencedSet <-- 新增接口
              ↑
         NavigableSet
```

## 记忆要点

- 核心痛点：Java 21前获取末尾元素API割裂，如List需算size减一，Map更是无直接方法。
- 三大统一接口：新增SequencedCollection, SequencedSet, SequencedMap。
- 核心方法：提供统一的getFirst()、addLast()及逆序遍历reversed()方法。
- 实战应用：为LinkedHashMap直接补充pollLastEntry()，让LRU淘汰逻辑从繁杂迭代变为一行代码。

## 结构化回答

**30 秒电梯演讲：** 统一所有集合访问首尾元素和逆序的API。打个比方，像给所有集装箱装上通用的“首尾门”和“倒车开关”，不管箱子类型。

**展开框架：**
1. **核心痛点** — Java 21前获取末尾元素API割裂，如List需算size减一，Map更是无直接方法。
2. **三大统一接口** — 新增SequencedCollection, SequencedSet, SequencedMap。
3. **核心方法** — 提供统一的getFirst()、addLast()及逆序遍历reversed()方法。

**收尾：** 我在项目里踩过坑——在实现“最近浏览记录”功能时，通常使用LinkedHashMap保持插入顺序并淘汰最旧数据。您想深入聊哪一段：原理、避坑还是对比选型？

## 视频脚本

> 预计时长：2 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：什么是Sequenced Colle… | "什么是Sequenced Collections（顺序集合）？Java 21引入了哪些新接口和方法？一句话——像给所有集装箱装上通用的“首尾门”和“倒车开关”，不管箱子类型。" | 开场钩子 |
| 0:40 | 概念动画/示意图 | "统一所有集合访问首尾元素和逆序的API——像给所有集装箱装上通用的“首尾门”和“倒车开关”，不管箱子类型" | 核心定义 |
| 1:20 | 核心痛点示意 | "Java 21前获取末尾元素API割裂，如List需算size减一，Map更是无直接方法。" | 要点1 |
| 2:00 | 总结卡 | "记住这几条，面试不慌。下期讲进阶追问。" | 收尾 |

---

## 延伸：JDK 21的Sequenced Collections是什么？解决了什么问题？

> 合并自 `sjdk-007`（相似度 65%）

🎯 本质：Sequenced Collections 是 JDK 21 引入的新接口体系，为有明确相遇顺序的集合提供统一的 API。

🔧 **问题背景**：之前获取第一个/最后一个元素的方式各不相同，缺乏统一接口导致代码难以泛化：
- `List`: `list.get(0)` / `list.get(list.size()-1)`
- `Deque`: `deque.getFirst()` / `deque.getLast()`
- `SortedSet`: `set.first()` / `set.last()`

**实战案例**：在编写通用的 LRU 缓存工具类时，以前需要分别处理 `ArrayList` 和 `LinkedHashSet` 的首尾移除逻辑，代码充斥着 `instanceof` 判断。使用 `SequencedCollection` 后，统一调用 `removeFirst()` 和 `addLast()` 即可，代码行数减少 40%。

**新接口层次架构图**：

```text
         Collection (Iterable)
               △
               │
      ┌────────┴─────────┐
      │                  │
      │         SequencedCollection
      │ (新增: addFirst, addLast, getFirst...)
      │                  △
      │      ┌───────────┴───────────┐
      │      │                       │
   List      │               SequencedSet (新增接口)
             │                       △
             │         ┌─────────────┴─────────────┐
             │         │                           │
        Deque    LinkedHashSet          SortedSet
                                     │
                                     ▼
                                  SequencedMap (新增接口)
                          (提供 firstEntry, lastEntry, reversed...)
```

核心方法：
- `getFirst()` / `getLast()` - 获取首尾元素（相当于 peek）
- `addFirst(e)` / `addLast(e)` - 头尾添加
- `removeFirst()` / `removeLast()` - 头尾删除
- `reversed()` - 反转视图（不修改原集合，返回反向视图）

使用示例：
```java
LinkedHashSet<String> set = new LinkedHashSet<>();
set.add("A"); set.add("B"); set.add("C");
set.getFirst(); // "A"
set.getLast();   // "C"
set.reversed();  // [C, B, A] 视图

SequencedMap<String, Integer> map = new LinkedHashMap<>();
map.putFirst("New", 1); // 新增方法
```

这是一个向后兼容的改进——所有现有的有序集合（`ArrayList`, `LinkedList`, `LinkedHashSet`, `TreeMap` 等）都新增实现了这些接口。

## 常见考点
1. **reversed() 返回的是新集合还是视图？**
   返回的是**视图**。修改视图会直接影响原集合，反之亦然。类似 `subList` 的行为。
2. **List 既然已经实现了 Collection，为什么还需要 SequencedCollection？**
   虽然 List 有顺序，但 `Set` 和 `Map` 的有序实现（如 LinkedHashSet）之前没有通用接口。为了统一所有“有序”容器，引入了中间接口 `SequencedCollection`，让 `List` 也实现它，从而允许泛型代码同时处理 List 和 LinkedHashSet。
3. **ArrayList 使用 addFirst 性能如何？**
   由于 `ArrayList` 底层是数组，`addFirst` 需要移动所有元素（System.arraycopy），时间复杂度为 O(N)。虽然 API 统一了，但在大数据量下仍需注意性能开销。

## 记忆要点

- 痛点：解决List、Deque、LinkedHashSet等有序集合首尾操作API各不相同的问题
- 核心：新增SequencedCollection统一接口，提供getFirst、addLast等方法
- reversed()：返回双向迭代视图而非新集合，修改视图直接影响原对象
- 避坑：ArrayList调用addFirst时间复杂度为O(N)，大数量批量操作慎用

## 结构化回答


**30 秒电梯演讲：** 给List、Set、Map都装上统一的“前门”和“后门”开关

**展开框架：**
1. **引入Sequen** — 引入SequencedCollection/Map接口
2. **统一getFir** — 统一getFirst/getLast/addFirst等操作
3. **reversed** — reversed()返回反向视图

**收尾：** 这是我实战中的理解，您想深入哪一段？


## 视频脚本

> 预计时长：2 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：JDK 21的Sequenced C… | "JDK 21的Sequenced Collections是什么？解决了什么问题？一句话——给List、Set、Map都装上统一的“前门”和“后门”开关。" | 开场钩子 |
| 0:40 | 概念动画/示意图 | "统一有序集合的首尾操作API——给List、Set、Map都装上统一的“前门”和“后门”开关" | 核心定义 |
| 1:20 | 痛点示意 | "解决List、Deque、LinkedHashSet等有序集合首尾操作API各不相同的问题" | 要点1 |
| 2:00 | 总结卡 | "记住这几条，面试不慌。下期讲进阶追问。" | 收尾 |
