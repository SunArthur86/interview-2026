---
id: sjdk-001
difficulty: L2
category: java-core
feynman:
  essence: JVM管理的轻量级线程，实现M:N调度，专为高并发IO设计。
  analogy: 像写代码时的“思维导图”，数量无限且切来切去不费内存。
  first_principle: 如何让Java以极低成本模拟海量并发操作？
  key_points:
  - 由JVM调度，非OS线程，打破1:1映射
  - 内存占用极低（KB级），单机可创建百万级
  - IO阻塞时自动卸载，释放载体线程
  - 不适合CPU密集型，禁止池化
memory_points:
- 本质：JDK 21轻量级线程，由JVM调度，打破Java线程与OS线程的1:1映射
- 对比：传统线程内存~MB级且数量受限，虚拟线程~KB级可达百万并发
- 原理：遇阻塞IO时JVM自动卸载，挂起不占用底层载体线程，专治IO密集型
- 避坑：不要池化且切忌用于CPU密集，遇synchronized会Pinning(钉住)载体线程
---

# 什么是Virtual Threads（虚拟线程）？它与传统线程有什么区别？

🎯 **本质**：虚拟线程是JDK 21引入的轻量级线程，由JVM管理而非操作系统，打破了Java线程与OS线程的1:1映射关系。

🧒 **类比**：传统线程就像出租车（昂贵、数量有限），虚拟线程就像共享单车（便宜、可以停满整条街）。

📊 **核心区别**：
| 特性 | 平台线程 | 虚拟线程 |
|------|---------|---------|
| 映射 | 1:1 OS线程 | N:M 调度到载体线程 |
| 内存 | ~1-2MB/线程 | ~几KB/线程 |
| 创建成本 | 高(系统调用) | 极低(纯JVM) |
| 数量上限 | 几百~几千 | 可达数百万 |
| 适用场景 | CPU密集型 | IO密集型 |

🔧 **工作原理（架构图）**：
```text
  应用代码              调度器              操作系统
┌─────────────┐      ┌───────────────────────┐     ┌───────────────┐
│ 虚拟线程 VT1 │───→  │ 载体线程 PT1 (Carrier) │────→ │ OS Thread 1   │
│ (I/O 操作)   │      │ (执行 VT1 的任务)      │     │               │
└─────────────┘      └───────────────────────┘     └───────────────┘
       │
       │ (遇到阻塞 I/O)
       │ JVM 将 VT1 从 PT1 上卸载
       ↓
┌─────────────┐      ┌───────────────────────┐
│ 虚拟线程 VT1 │ (Heap)│ 载体线程 PT1 闲置      │────→ │ OS Thread 1   │
│ (挂起状态)   │      │ 可立即调度 VT2        │     │ (运行 VT2)    │
└─────────────┘      └───────────────────────┘     └───────────────┘
```

💻 **使用方式**：
```java
try (var executor = Executors.newVirtualThreadPerTaskExecutor()) {
    IntStream.range(0, 100_000).forEach(i → executor.submit(() → {
        Thread.sleep(Duration.ofSeconds(1)); // 模拟IO阻塞
        return i;
    }));
}
```

**实战案例**：在重构一个高并发网关服务时，原先使用传统线程池处理请求，由于等待下游服务响应（阻塞IO），线程池很快耗尽导致大量拒绝请求。迁移到虚拟线程后，我们创建了10万个虚拟线程处理并发请求，物理CPU核心数未变，但吞吐量提升了3倍，且不再需要维护复杂的ThreadPool参数调优。

⚠️ **注意事项**：
1. 不适合CPU密集型任务
2. 不要池化虚拟线程——用完即弃
3. synchronized块内IO会pin载体线程，建议用ReentrantLock
4. ThreadLocal在百万虚拟线程中会导致内存问题，考虑Scoped Values

## 常见考点
1. **虚拟线程是绿色线程吗？有什么区别？**
   - 概念相似，都是用户态线程。区别在于：早期的绿色线程（JDK 1.0）是 M:1 映射（所有绿色线程映射到一个 OS 线程），无法利用多核；Java 虚拟线程是 M:N 映射，可以利用所有可用的 CPU 核心。
2. **载体线程是固定的吗？**
   - 不固定。虚拟线程在生命周期中会被 JVM 调度到不同的载体线程上运行。因此，在虚拟线程中调用 `Thread.currentThread()` 返回的永远是该虚拟线程本身，而非底层的载体线程。

## 记忆要点

- 本质：JDK 21轻量级线程，由JVM调度，打破Java线程与OS线程的1:1映射
- 对比：传统线程内存~MB级且数量受限，虚拟线程~KB级可达百万并发
- 原理：遇阻塞IO时JVM自动卸载，挂起不占用底层载体线程，专治IO密集型
- 避坑：不要池化且切忌用于CPU密集，遇synchronized会Pinning(钉住)载体线程

## 结构化回答

**30 秒电梯演讲：** JVM管理的轻量级线程，实现M:N调度，专为高并发IO设计。打个比方，像写代码时的“思维导图”，数量无限且切来切去不费内存。

**展开框架：**
1. **本质** — JDK 21轻量级线程，由JVM调度，打破Java线程与OS线程的1:1映射
2. **对比** — 传统线程内存~MB级且数量受限，虚拟线程~KB级可达百万并发
3. **原理** — 遇阻塞IO时JVM自动卸载，挂起不占用底层载体线程，专治IO密集型

**收尾：** 我在项目里踩过坑——synchronized块内IO会pin载体线程，建议用ReentrantLock。您想深入聊哪一段：原理、避坑还是对比选型？

## 视频脚本

> 预计时长：2 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：什么是Virtual Threads… | "什么是Virtual Threads（虚拟线程）？它与传统线程有什么区别？一句话——像写代码时的“思维导图”，数量无限且切来切去不费内存。" | 开场钩子 |
| 0:40 | 概念动画/示意图 | "JVM管理的轻量级线程，实现M:N调度，专为高并发IO设计——像写代码时的“思维导图”，数量无限且切来切去不费内存" | 核心定义 |
| 1:20 | 本质示意 | "JDK 21轻量级线程，由JVM调度，打破Java线程与OS线程的1:1映射" | 要点1 |
| 2:00 | 总结卡 | "记住这几条，面试不慌。下期讲进阶追问。" | 收尾 |
