---
id: tr3-008
difficulty: L2
category: middleware
subcategory: 可观测性
feynman:
  essence: 线程池丢失上下文，用装饰器包装执行器自动搬运TraceId。
  analogy: 主线程像接力赛传递棒子，默认跨线程时棒子掉地上；`ContextPropagatingExecutorService`就像一个自动机械臂，把主线程的棒子（TraceId）稳稳抓取并交给新线程。
  first_principle: 线程隔离导致ThreadLocal变量无法自动继承，需显式传播。
  key_points:
  - 基于ThreadLocal的上下文默认不自动跨线程
  - 手动线程池需用`ContextPropagatingExecutorService`包装
  - 包装器负责在任务提交和执行时捕获恢复上下文
memory_points:
- 上下文存储于ThreadLocal，导致跨线程池时极易发生Trace丢失问题。
- '@Async注解默认已支持自动传播，但手动创建的线程池会导致丢失。'
- 解决方案：使用ContextPropagatingExecutorService包装线程池。
- 机制是在任务提交时先快照捕获上下文，执行任务时再恢复上下文。
---

# Micrometer Tracing与Spring Boot 3.x集成后，如何解决线程池异步任务中的Trace上下文丢失问题？

在Spring Boot 3.x中，Micrometer Tracing基于ThreadLocal管理上下文。默认情况下，`@Async`自动传播上下文，但手动创建的线程池会导致上下文丢失。解决方案：手动线程池需使用`ContextPropagatingExecutorService`包装原有`ExecutorService`，在任务提交时捕获并在执行时恢复上下文。

## 记忆要点

- 上下文存储于ThreadLocal，导致跨线程池时极易发生Trace丢失问题。
- @Async注解默认已支持自动传播，但手动创建的线程池会导致丢失。
- 解决方案：使用ContextPropagatingExecutorService包装线程池。
- 机制是在任务提交时先快照捕获上下文，执行任务时再恢复上下文。

## 结构化回答

**30 秒电梯演讲：** 线程池丢失上下文，用装饰器包装执行器自动搬运TraceId。打个比方，主线程像接力赛传递棒子，默认跨线程时棒子掉地上；`ContextPropagatingExecutorService`就像一个自动机械臂，把主线程的棒子（TraceId）稳稳抓取并交给新线程。

**展开框架：**
1. **上下文存储于ThreadLocal** — 导致跨线程池时极易发生Trace丢失问题。
2. **@Async注解默认已支持自动传播** — 但手动创建的线程池会导致丢失。
3. **解决方案** — 使用ContextPropagatingExecutorService包装线程池。

**收尾：** 这三点都能配合实战聊。您想深入聊原理、对比还是避坑？

## 视频脚本

> 预计时长：2 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：Micrometer Tracing… | "Micrometer Tracing与Spring Boot 3.x集成后，如何解决线程池异步任务中的Trace上下文丢失问题？一句话——主线程像接力赛传递棒子，默认跨线程时棒子掉地上；`ContextPropagatingExecutorService`就像一个自动机械臂，把主线程的棒子（TraceId）稳稳抓取并交给新线程。" | 开场钩子 |
| 0:40 | 概念动画/示意图 | "线程池丢失上下文，用装饰器包装执行器自动搬运TraceId——主线程像接力赛传递棒子，默认跨线程时棒子掉地上；`ContextPropagatingExecutorService`就像一个自动机械臂，把主线程的棒子（TraceId）稳稳抓取并交给新线程" | 核心定义 |
| 1:20 | 要点1图解示意 | "导致跨线程池时极易发生Trace丢失问题。" | 要点1 |
| 2:00 | 总结卡 | "记住这几条，面试不慌。下期讲进阶追问。" | 收尾 |
