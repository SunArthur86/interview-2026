---
id: lln-059
difficulty: L2
category: llm-core
subcategory: 大模型综合
images:
- llmnotes_p082_img1.jpg
feynman:
  essence: 通过限制候选集和调节概率分布平滑度，平衡生成结果的确定性与多样性。
  analogy: 像抽奖，Top-k是只从前几名里抽，Top-p是抽到累积金额够数为止，Temperature是让奖项金额差距变大还是变小。
  first_principle: 如何从模型输出的概率分布中采样，以生成既合理又多样的文本？
  key_points:
  - Top-k限制候选数量，Top-p限制累积概率阈值
  - Temperature控制概率分布的陡峭程度
  - 低T用于精确任务，高T用于创意任务
follow_up:
- Beam Search为什么在开放域生成中效果不好?
- Contrastive Search是如何平衡质量和多样性的?
- 什么是repetition penalty和frequency penalty?
memory_points:
- Temperature：控制分布尖锐度。T越小越确定（事实问答），T越大越随机（创意写作）。
- Top-k：硬性截断，只保留概率最高的前k个Token进行采样，易受分布长尾影响。
- Top-p (Nucleus)：动态截断，保留累积概率达到p的最小集合，比Top-k更灵活智能。
- 选型建议：高质量复杂代码与JSON生成用Greedy或极低T；自然语言对话多用Top-p结合适度T。
frequency: low
---

# 大模型的解码策略有哪些?Top-k、Top-p和Temperature的作用是什么

解码策略决定了模型如何从概率分布中选择下一个token。

### 主要策略对比

| 策略 | 原理 | 特点 |
|------|------|------|
| **Greedy** | 每步选概率最高的token | 确定性,容易重复 |
| **Beam Search** | 维护K个候选序列 | 质量高但多样性低 |
| **Top-k** | 只从概率最高的k个token中采样 | 限制选择范围 |
| **Top-p (Nucleus)** | 从累积概率达到p的最小token集合中采样 | 动态调整范围 |
| **Temperature** | 调整概率分布的尖锐程度 | 控制随机性 |

### Temperature 详解
公式：$P'(y_i) = \text{softmax}(\text{logit}_i / T)$
- **$T \to 0$**：分布更尖锐（接近 Greedy），输出更确定。
- **$T = 1$**：原始概率分布。
- **$T \to \infty$**：分布更平坦（接近均匀随机），输出更随机。

### Top-p (Nucleus Sampling)
- 将 token 按概率降序排列。
- 选择累积概率达到 $p$ 的最小 token 集合。
- $p=0.9$：只在累积概率 90% 的 token 中采样。
- 比Top-k更灵活：概率集中时选少量token，概率分散时选更多。

```text
      采样策略可视化对比 (假设输出词表只有5个词)

      原始概率分布 (Temperature=1.0)
      Token:  A(0.4)  B(0.3)  C(0.2)  D(0.05) E(0.05)
      ██████ █████ ████ █ █

      Top-k (k=3)      Top-p (p=0.9)          Temperature > 1
      选择: A, B, C     选择: A, B, C           概率平滑
      D, E 概率置0     (0.4+0.3+0.2=0.9)      A, B 差距变小
                                            D, E 概率上升
```

### 实践推荐
- **事实问答**：Temperature=0.1, Top_p=0.95
- **创意写作**：Temperature=0.8, Top_p=0.9
- **代码生成**：Temperature=0.2, Top_p=0.95

## 常见考点
1. **Repetition Penalty 是什么？**：在解码过程中，如果已经生成了某些token，人为降低这些token再次出现的概率（通常除以一个系数 > 1），防止模型生成重复的文本循环。
2. **为什么在开放式生成中很少用 Beam Search？**：Beam Search 容易陷入重复循环（如 "The cat is on the mat. The cat is on the mat..."），且生成的文本缺乏人类的随机性和多样性，相比 Sampling 策略显得过于呆板。
3. **Top-k 和 Top-p 可以同时使用吗？**：可以。通常会先筛选出概率最高的 k 个 token，再在这 k 个 token 中应用 Top-p 进行采样（或者反之），两者结合可以进一步过滤掉噪声。

**【实战案例】**
在构建 Agent 工具调用系统时，使用 Greedy Search 导致 JSON 字段经常解析失败（如生成括号不匹配）。引入 **Top-p (p=0.9)** 后，虽然格式正确率提升，但偶尔出现幻觉关键字。最终优化方案是：Temperature=0（保证格式严谨），但在 **Prompt** 中强制增加结构化输出示例，效果比单纯调参更稳定。另一个坑是：`repetition_penalty` 设置过高（如 > 1.2）会导致模型在生成代码时变量名随机变更，甚至产生不存在的变量引用。

**【代码示例】(Python/Transformers)**
```python
from transformers import AutoModelForCausalLM, AutoTokenizer

tokenizer = AutoTokenizer.from_pretrained("gpt2")
model = AutoModelForCausalLM.from_pretrained("gpt2")

input_text = "The future of AI is"
inputs = tokenizer(input_text, return_tensors="pt")

# 关键参数配置
outputs = model.generate(
    **inputs,
    max_new_tokens=50,
    do_sample=True,           # 必须开启才能使用 Top-p / Top-k
    top_k=50,                 # 限制候选词数量，过滤掉长尾噪声
    top_p=0.95,               # 核积采样，保证多样性
    temperature=0.7,          # 控制创造性
    repetition_penalty=1.1    # 防止复读机
)
print(tokenizer.decode(outputs[0], skip_special_tokens=True))
```

## 流程图

```mermaid
flowchart TD
    classDef start fill:#4CAF50,color:#fff
    classDef process fill:#2196F3,color:#fff
    classDef decision fill:#FF9800,color:#fff
    classDef special fill:#9C27B0,color:#fff
    classDef error fill:#f44336,color:#fff
    classDef info fill:#607D8B,color:#fff
    class A start
    class B process
    class C decision
    class D special
    class D1 error
    class D2 info
    class D3 start
    class E1 process
    class E2 decision
    class E3 special
    class Greedy error
    class Logits info
    class Softmax start
    class T process
    class Top decision
    class k special
    class p error
    A[模型 Logits] --> B[/温度 T 调节概率分布]
    B --> C[Softmax 概率]
    C --> D{采样策略}
    D --> D1[Greedy: 取最大]
    D --> D2[Top-k: 限定 k 个候选]
    D --> D3[Top-p: 累积概率截断]
    D1 --> E1[确定 重复]
    D2 --> E2[可控多样性]
    D3 --> E3[自适应多样性]
```

## 记忆要点

- Temperature：控制分布尖锐度。T越小越确定（事实问答），T越大越随机（创意写作）。
- Top-k：硬性截断，只保留概率最高的前k个Token进行采样，易受分布长尾影响。
- Top-p (Nucleus)：动态截断，保留累积概率达到p的最小集合，比Top-k更灵活智能。
- 选型建议：高质量复杂代码与JSON生成用Greedy或极低T；自然语言对话多用Top-p结合适度T。

## 结构化回答

**30 秒电梯演讲：** 像抽奖，Top-k是只从前几名里抽，Top-p是抽到累积金额够数为止，Temperature是让奖项金额差距变大还是变小。

**展开框架：**
1. **Top** — Top-k限制候选数量，Top-p限制累积概率阈值
2. **Temperature** — Temperature控制概率分布的陡峭程度
3. **低T** — 低T用于精确任务，高T用于创意任务

**收尾：** Beam Search为什么在开放域生成中效果不好？

## 视频脚本

> 预计时长：2 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡 | "大模型的解码策略有哪些，30 秒讲清楚。" | 开场钩子 |
| 0:30 | 概念定义动画 | "一句话：通过限制候选集和调节概率分布平滑度，平衡生成结果的确定性与多样性。" | 核心定义 |
| 1:00 | Temperature图解 | "控制分布尖锐度。T越小越确定（事实问答），T越大越随机（创意写作）。" | Temperature |
| 1:30 | 总结卡 | "记好这几条，面试不慌。下期见。" | 收尾 |

### 视频流程图

```mermaid
flowchart LR

    subgraph Intro["🎥 引入"]
        N0["大模型的解码策略有哪些<br/>0:00"]:::intro
    end

    subgraph Core["📖 核心讲解"]
        N1["通过限制候选集和调节概率分布平滑度，平衡生成结果<br/>0:30"]:::core
    end

    subgraph Practice["🔧 实战"]
        N2["Temperature图解<br/>1:00"]:::practice
    end

    subgraph Wrap["🎬 收尾"]
        N3["总结回顾 & 下期预告<br/>1:30"]:::wrap
    end

    N0 --> N1 --> N2 --> N3

    classDef intro fill:#FF9800,color:#fff
    classDef core fill:#2196F3,color:#fff
    classDef deep fill:#4CAF50,color:#fff
    classDef practice fill:#9C27B0,color:#fff
    classDef wrap fill:#607D8B,color:#fff
```


