---
id: llm--015
difficulty: L1
category: llm-core
subcategory: 大模型基础
tags:
- IO
- Elasticsearch
feynman:
  essence: 用户与模型交互的指令文本，用于引导模型生成特定输出。
  analogy: 给下属布置任务，角色越明确、要求越具体，结果越好。
  first_principle: 如何准确传达人类意图以激活模型对应的知识区域？
  key_points:
  - 包含角色设定、任务描述、上下文
  - 需明确指定输出格式和约束条件
  - 本质是激发模型预训练知识的线索
  - Prompt Engineering 是提升效果的关键
follow_up:
- Few-shot 和 Zero-shot 有什么区别?
- 如何衡量 Prompt 的质量?
memory_points:
- 定义：输入给模型的指令文本，指导做什么和怎么做。
- 五要素：角色设定、明确任务、背景上下文、输出格式、约束条件。
- 核心原则：清晰具体的Prompt能显著提升回答精准度。
- 技巧：使用分隔符(如XML标签)防止指令注入。
---

# 什么是 Prompt(提示词)?好的 Prompt 有什么特点

- **Prompt** 是输入给大模型的指令文本,告诉模型'做什么'和'怎么做'.

- **好的 Prompt 的 5 个要素**
1. **角色**:'你是一个资深 Python 工程师'
2. **任务**:'请审查以下代码的安全漏洞'
3. **上下文**:提供背景信息/数据
4. **格式**:'以 JSON 格式输出,包含 severity 和 description'
5. **约束**:'不超过 200 字,不要编造信息'

- **一句话理解**:Prompt 是你和大模型沟通的'语言',写得越清晰,回答越精准.

- **实战案例**：在开发企业级知识库问答时，曾遇到模型将用户指令误认为检索内容的问题。通过在 Prompt 中添加明确的 XML 标签（如 `<user_input>` 和 `<context>`）分隔符，成功消除了指令注入风险，解析成功率提升至 99%。

- **代码示例**：
```python
# LangChain 风格的结构化 Prompt 模板
prompt_template = """
你是一个{role}。
请根据以下上下文回答问题：
<context>
{context}
</context>

问题：{question}

要求：以 JSON 格式输出，仅包含 'answer' 和 'source' 字段。
"""
```

## 记忆要点

- 定义：输入给模型的指令文本，指导做什么和怎么做。
- 五要素：角色设定、明确任务、背景上下文、输出格式、约束条件。
- 核心原则：清晰具体的Prompt能显著提升回答精准度。
- 技巧：使用分隔符(如XML标签)防止指令注入。


## 结构化回答

**30 秒电梯演讲：** 用户与模型交互的指令文本，用于引导模型生成特定输出。——打个比方，给下属布置任务，角色越明确、要求越具体，结果越好。

**展开框架：**
1. **定义** — 输入给模型的指令文本，指导做什么和怎么做。
2. **五要素** — 角色设定、明确任务、背景上下文、输出格式、约束条件。
3. **核心原则** — 清晰具体的Prompt能显著提升回答精准度。

**收尾：** 以上三点都能配合实战聊。我可以展开任一要点，比如「Few-shot 和 Zero-shot 有什么区别」这类追问您感兴趣吗？

## 视频脚本

> 预计时长：2 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡 | "Prompt(提示词)，30 秒讲清楚。" | 开场钩子 |
| 0:30 | 概念定义动画 | "一句话：用户与模型交互的指令文本，用于引导模型生成特定输出。" | 核心定义 |
| 1:00 | 定义图解 | "输入给模型的指令文本，指导做什么和怎么做。" | 定义 |
| 1:30 | 总结卡 | "记好这几条，面试不慌。下期见。" | 收尾 |
