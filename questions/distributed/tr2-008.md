---
id: tr2-008
difficulty: L2
category: distributed
subcategory: 分布式事务
feynman:
  essence: 代理数据源解析SQL存镜像，自动生成补偿SQL回滚
  analogy: 像打游戏自动存档，操作前记录状态，搞砸了系统自动读档还原，不用你手动重来。
  first_principle: 如何在不修改业务代码的前提下保证分布式事务的最终一致性？
  key_points:
  - 数据源代理实现无侵入
  - 一阶段记录前后镜像构建回滚日志
  - 二阶段根据镜像自动生成补偿SQL
memory_points:
- 无侵入本质：因为Seata代理了JDBC数据源，所以业务无感知直接用本地事务。
- 一阶段：拦截SQL解析，记录修改前后的前镜像与后镜像，生成回滚日志随业务一并提交。
- 二阶段回滚：取出前镜像生成反向SQL更新，若对比当前数据有脏写则结合全局锁校验补偿。
---

# Seata 分布式事务的 AT 模式是如何实现“无侵入”的？在一阶段本地事务提交后，如果二阶段全局回滚，它是利用什么机制自动生成回滚 SQL 的？

Seata AT 模式的无侵入性源于对 JDBC 数据源的代理，业务代码使用普通 `@Transactional` 即可。一阶段本地事务提交时，Seata 拦截 SQL，记录修改前后的“前镜像”和“后镜像”生成回滚日志，随业务数据一并提交，保证原子性。二阶段全局回滚时，Seata 利用日志中的“前镜像”和当前数据的“后镜像”对比：若无脏写直接生成反向 SQL；若有脏写则结合全局锁校验，生成补偿 SQL 将数据还原至前镜像状态，实现自动回滚。

## 记忆要点

- 无侵入本质：因为Seata代理了JDBC数据源，所以业务无感知直接用本地事务。
- 一阶段：拦截SQL解析，记录修改前后的前镜像与后镜像，生成回滚日志随业务一并提交。
- 二阶段回滚：取出前镜像生成反向SQL更新，若对比当前数据有脏写则结合全局锁校验补偿。

## 结构化回答




**30 秒电梯演讲：** 像打游戏自动存档，操作前记录状态，搞砸了系统自动读档还原，不用你手动重来。

**展开框架：**
1. **数据源代理实** — 数据源代理实现无侵入
2. **一阶段记录前** — 一阶段记录前后镜像构建回滚日志
3. **SQL** — 二阶段根据镜像自动生成补偿SQL

**收尾：** 这是我实战中的理解，您想深入哪一段？




## 视频脚本

> 预计时长：2 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：Seata 分布式事务的 AT 模式是如 | "Seata 分布式事务的 AT 模式是如，一分钟讲透。" | 开场钩子 |
| 0:35 | 生活类比动画 | "打个比方——像打游戏自动存档，操作前记录状态，搞砸了系统自动读档还原，不用你手动重来。" | 核心类比 |
| 1:10 | 概念定义动画 | "一句话：代理数据源解析SQL存镜像，自动生成补偿SQL回滚。" | 核心定义 |
| 1:50 | 数据源代理 图解 | "数据源代理实现无侵入。" | 数据源代理 |
