---
id: scn-008
difficulty: L3
category: distributed
feynman:
  essence: 区分容器OOM（系统杀进程）和JVM OOM（内存不足），针对性排查。
  analogy: 像房东因超租把房客赶走（容器OOM），和房客东西多房间放不下（JVM OOM）。
  first_principle: 如何排查JVM在受限容器资源下的内存崩溃问题？
  key_points:
  - 容器OOM需调整JVM内存适配容器
  - JVM Heap OOM需分析堆Dump查泄漏
  - 使用MaxRAMPercentage动态计算堆内存
  - 利用JFR或Arthas进行性能诊断
memory_points:
- OOM层级区分：JVM OOM是堆溢出抛异常(需Dump分析)，而容器OOMKilled是总内存超Cgroup被系统杀进程。
- 内存比例：因为JVM非堆内存需开销，所以堆内存建议配置MaxRAMPercentage=75%而非占满Limit。
- 排查指令：堆溢出用HeapDumpOnOutOfMemoryError生成hprof文件分析，而堆外内存溢出需开启NativeMemoryTracking。
frequency: high
---

# Java应用在容器中常见的OOM和性能问题有哪些？如何排查？

🎯 本质：容器环境下的Java内存管理受 Cgroup 限制，容易出现容器 OOM Killed（系统杀进程）和 JVM OOM（应用抛异常）两种不同层级的问题。

**1. 常见内存问题分类**

**A. 容器 OOM Killed (Cgroup 层面)**
- **现象**：Pod 状态为 `OOMKilled`，退出码 137 (128 + 9 SIGKILL)，应用日志可能戛然而止。
- **原因**：JVM 总内存（Heap + Metaspace + Direct Memory + Stack + Code Cache + GC开销）超过了 K8s 设置的 `limits.memory`。
- **排查**：`kubectl describe pod <pod-name>` 查看 `Last State: Terminated (OOMKilled)`。
- **解决**：
  - 使用 `-XX:MaxRAMPercentage=75.0` 动态限制堆大小。
  - 留出足够内存给非堆区域（建议预留 20-30%）。

**B. JVM Heap OOM (应用层面)**
- **现象**：日志抛出 `java.lang.OutOfMemoryError: Java heap space`。
- **原因**：内存泄漏或堆内对象过多，GC 无法回收。
- **排查**：
  - 启动参数加 `-XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/logs/heap.hprof`。
  - 使用 MAT (Memory Analyzer Tool) 分析 Dump 文件，找 Retained Heap 最大的对象。

**C. Metaspace OOM**
- **现象**：`OutOfMemoryError: Metaspace`。
- **原因**：动态加载的类过多（如反射生成代理类、Groovy脚本、大量JSP）。
- **解决**：调整 `-XX:MaxMetaspaceSize=256m`，排查是否存在类加载泄漏。

**D. Direct Memory OOM**
- **现象**：`OutOfMemoryError: Direct buffer memory`。
- **原因**：Netty/NIO 堆外内存未释放，或未设置限制。
- **解决**：`-XX:MaxDirectMemorySize=256m`，开启 Native Memory Tracking (`-XX:NativeMemoryTracking=detail`) 排查。

**E. GC overhead limit**
- **现象**：`java.lang.OutOfMemoryError: GC overhead limit exceeded`。
- **原因**：应用运行时间 98% 都在做 GC，回收的内存少于 2%。
- **解决**：检查内存泄漏，或扩容 Heap，或调整 GC 策略（如从 Serial 切换到 G1）。

**2. 容器内存布局与计算**
```
┌──────────────────────────────────────────────────┐
│  Container Limit (K8s Limit, e.g., 4GB)          │
├──────────────────────────────────────────────────┤
│  JVM Heap (-Xmx or MaxRAMPercentage ~75%)        │
│  (Java Objects)                                  │
├──────────────────────────────────────────────────┤
│  Non-Heap Memory (~25%)                          │
│  - Metaspace (Class metadata)                    │
│  - Direct Memory (NIO/Netty buffers)             │
│  - Thread Stacks (Xss * 线程数)                  │
│  - Code Cache (JIT compiled code)                │
│  - GC Structures (StringDedup, etc.)             │
│  - Other (OS overhead, Socket buffers)           │
└──────────────────────────────────────────────────┘
```

**3. 关键启动参数配置**
```bash
# 推荐配置 (Java 8u191+ / Java 11+ 支持容器感知)
java -jar app.jar \
  -XX:+UseContainerSupport \
  -XX:MaxRAMPercentage=75.0 \
  -XX:InitialRAMPercentage=75.0 \
  -XX:+HeapDumpOnOutOfMemoryError \
  -XX:HeapDumpPath=/tmp/heapdump.hprof \
  -XX:+PrintGCDetails -XX:+PrintGCDateStamps
```

**实战案例**：某服务在容器中重启，排查发现是堆外内存溢出导致容器被杀。由于未设置 `-XX:MaxDirectMemorySize`，默认等同于 Max Heap，导致 Netty 分配大量堆外 Buffer 时突破了容器 Limit。解决方案是将 MaxRAMPercentage 调至 60%，并为非堆内存预留足够空间。


## 核心流程图

```mermaid
flowchart TD
    REQ([请求进入]) --> INPUT[输入处理<br/>原始请求]
    INPUT --> CORE{核心判断}
    CORE -->|正常路径| NORMAL[主流程执行<br/>遵循 CAP/BASE 约束]
    CORE -->|异常路径| ABNORMAL[异常分支<br/>故障转移/重试]

    NORMAL --> COORD[协调者/主节点<br/>统一调度]
    COORD --> REPLICATE[数据复制<br/>多副本一致性]
    REPLICATE --> CONSISTENCY{一致性协议}
    CONSISTENCY -->|强一致 CP| STRONG[Raft/Paxos/ZAB<br/>多数派 quorum]
    CONSISTENCY -->|最终一致 AP| EVENTUAL[异步复制<br/>Dynamo/Cassandra]

    ABNORMAL --> TIMEOUT[超时检测]
    TIMEOUT --> RETRY[指数退避重试]
    RETRY --> FAIL_FAST[快速失败<br/>触发降级]

    NORMAL --> OBS[可观测性<br/>Trace/Metric/Log]
    ABNORMAL --> OBS
    OBS --> RESP([返回响应])

    style REQ fill:#4CAF50,color:#fff
    style RESP fill:#2196F3,color:#fff
    style STRONG fill:#FF9800,color:#fff
    style EVENTUAL fill:#9C27B0,color:#fff
    style FAIL_FAST fill:#F44336,color:#fff
    style COORD fill:#009688,color:#fff

```

## 记忆要点

- OOM层级区分：JVM OOM是堆溢出抛异常(需Dump分析)，而容器OOMKilled是总内存超Cgroup被系统杀进程。
- 内存比例：因为JVM非堆内存需开销，所以堆内存建议配置MaxRAMPercentage=75%而非占满Limit。
- 排查指令：堆溢出用HeapDumpOnOutOfMemoryError生成hprof文件分析，而堆外内存溢出需开启NativeMemoryTracking。

## 结构化回答


**30 秒电梯演讲：** 像房东因超租把房客赶走（容器OOM），和房客东西多房间放不下（JVM OOM）。

**展开框架：**
1. **容器OOM需调整** — 容器OOM需调整JVM内存适配容器
2. **JVM Heap** — OOM需分析堆Dump查泄漏
3. **使用MaxRAM** — 使用MaxRAMPercentage动态计算堆内存

**收尾：** 这是我实战中的理解，您想深入哪一段？


## 视频脚本

> 预计时长：3 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：Java应用在容器中常见的OOM和性能问 | "Java应用在容器中常见的OOM和性能问，这题我会分三步讲。" | 开场钩子 |
| 0:41 | 概念定义动画 | "一句话：区分容器OOM(系统杀进程)和JVM OOM(内存不足)，针对性排查。" | 核心定义 |
| 1:22 | 生活类比动画 | "打个比方——像房东因超租把房客赶走(容器OOM)，和房客东西多房间放不下(JVM OOM)。" | 核心类比 |
| 2:03 | 容器OOM需调整 图解 | "容器OOM需调整JVM内存适配容器。" | 容器OOM需调整 |
| 2:50 | JVM Heap 图解 | "JVM Heap OOM需分析堆Dump查泄漏。" | JVM Heap |
