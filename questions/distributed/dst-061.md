---
id: dst-061
difficulty: L2
category: distributed
subcategory: 分布式基础
feynman:
  essence: 增强型2PC，一阶段提交并记录快照，二阶段异步提交或自动回滚。
  analogy: 像存档打游戏，每过一关自动存档，输了就读档重来。
  first_principle: 如何在保持数据库高并发性能的同时实现分布式事务的一致性？
  key_points:
  - 必须基于支持 ACID 的数据库（如 MySQL InnoDB）
  - Java 应用并通过 JDBC 访问
  - 一阶段本地提交即释放锁
  - 二阶段提交异步化，回滚依赖 Undo Log
memory_points:
- 前提约束：必须是基于支持ACID的关系型DB（如MySQL需InnoDB引擎）且通过JDBC连接。
- 表结构限制：必须有主键，且禁止外键级联与Text/Blob大字段。
- 一阶段无锁设计：拦截SQL生成前后镜像与Undo Log，提交即释放数据库锁。
- 隔离考点：非完全无锁，一阶段提交前需向TC申请并持有全局行锁实现写隔离。
---

# Seata AT模式的使用前提

Seata AT 模式（Automatic Transaction Mode）是增强型的二阶段提交协议，旨在保持强一致性的语义下提升性能。

### 核心特点
- **无锁设计**：不同于 XA 在一阶段 Prepare 后一直持有数据库锁，AT 模式在一阶段提交本地事务后立即释放数据库锁和连接。
- **自动化补偿**：通过 JDBC 拦截 SQL 自动解析，生成回滚日志，开发者无需手动编写补偿逻辑。

### 使用前提
1. **数据库支持**：
   - 必须基于支持本地 ACID 事务的关系型数据库。
   - **关键限制**：例如 MySQL 必须使用 **InnoDB** 引擎。MyISAM 引擎不支持事务，因此无法使用 Seata AT 模式。
2. **应用环境**：
   - Java 应用（JDK 1.8+）。
   - 必须通过 **JDBC** 访问数据库。因为 Seata 需要对数据源进行代理，通过拦截 JDBC 语句来解析 SQL 并生成前后镜像。如果是 ORM 框架（如 MyBatis/JPA）最终也是基于 JDBC 实现。
3. **表结构支持**：
   - 业务表必须包含主键（用于定位行记录）。
   - 禁止使用外键级联（因为跨表操作在分布式事务中难以原子性回滚）。
   - 业务表中不能存在类型为 Text/Blob 的大字段（可能会导致 Undo Log 存储溢出）。

### 模式流程（两阶段提交演变）
- **一阶段**：
  - 解析 SQL，查询“前镜像”。
  - 执行业务 SQL。
  - 查询“后镜像”。
  - 生成行锁（用于全局写隔离）。
  - 生成 Undo Log（包含前后镜像）。
  - 提交本地事务（业务数据 + Undo Log + 行锁），**释放本地数据库锁**。
- **二阶段**：
  - **提交**：异步删除 Undo Log，快速释放资源。
  - **回滚**：通过一阶段的回滚日志生成反向 SQL（补偿），并校验数据是否被脏写。

### 实战案例
在开发财务对账系统时，曾遇到过将表字段类型从 `VARCHAR` 改为 `TEXT` 导致 AT 模式回滚失败的问题。原因是 `undo_log` 表的序列化存储超限，导致二阶段回滚时反序列化异常，最终被迫回滚字段修改或改用 Seata TCC 模式。

### 常见考点
1. **AT 模式是否真的完全无锁？**：不是。AT 模式引入了“全局锁”的概念。在本地事务提交前，需要向 TC 申请全局锁，如果拿不到全局锁（说明其他全局事务正在修改），会一直重试，直到拿锁或超时回滚。
2. **支持哪些数据库？**：目前官方支持 MySQL、Oracle、PostgreSQL、SQL Server 等，但需要依赖 Undo Log 表的建表脚本支持。
3. **性能瓶颈在哪里？**：虽然一阶段释放了 DB 锁，但解析 SQL、生成镜像、全局锁的获取与网络通信仍会带来性能损耗，不适合高并发且对一致性要求极高的场景（如秒杀）。

## 记忆要点

- 前提约束：必须是基于支持ACID的关系型DB（如MySQL需InnoDB引擎）且通过JDBC连接。
- 表结构限制：必须有主键，且禁止外键级联与Text/Blob大字段。
- 一阶段无锁设计：拦截SQL生成前后镜像与Undo Log，提交即释放数据库锁。
- 隔离考点：非完全无锁，一阶段提交前需向TC申请并持有全局行锁实现写隔离。

## 结构化回答




**30 秒电梯演讲：** 像存档打游戏，每过一关自动存档，输了就读档重来。

**展开框架：**
1. **ACID** — 必须基于支持 ACID 的数据库（如 MySQL InnoDB）
2. **Java** — Java 应用并通过 JDBC 访问
3. **一阶段本地提** — 一阶段本地提交即释放锁

**收尾：** 这是我实战中的理解，您想深入哪一段？




## 视频脚本

> 预计时长：2 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：Seata AT模式的使用前提 | "Seata AT模式的使用前提，一分钟讲透。" | 开场钩子 |
| 0:35 | 生活类比动画 | "打个比方——像存档打游戏，每过一关自动存档，输了就读档重来。" | 核心类比 |
| 1:10 | 概念定义动画 | "一句话：增强型2PC，一阶段提交并记录快照，二阶段异步提交或自动回滚。" | 核心定义 |
| 1:50 | 必须 图解 | "必须基于支持 ACID 的数据库(如 MySQL InnoDB)。" | 必须 |
