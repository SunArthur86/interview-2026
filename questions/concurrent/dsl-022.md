---
id: dsl-022
difficulty: L2
category: concurrent
subcategory: 线程与锁
tags:
- 并发
feynman:
  essence: 进程是资源容器，线程是执行单元。
  analogy: 进程像工厂，线程像工厂里的工人。多个工人共享工厂资源（线程共享内存），但不同工厂资源独立（进程隔离）。
  first_principle: 如何平衡资源隔离与执行效率？
  key_points:
  - 资源分配与调度单位
  - 内存共享与隔离
  - 开销与通信差异
  - 稳定性与安全性影响
memory_points:
- 一句话定义：进程是资源分配的最小单位，而线程是 CPU 调度的最小单位。
- 资源对比：进程拥有独立内存，而线程共享所属进程的堆内存与文件句柄，仅独享栈和 PC。
- 开销对比：进程切换需刷新 TLB 开销极大，而线程切换开销极小。
- 健壮性对比：进程间内存强隔离互不影响，而线程一旦崩溃会导致整个进程崩溃。
---

# 进程和线程的区别

【进程 vs 线程：核心概念】
- **定义**：
  - **进程**：资源分配的最小单位。拥有独立的地址空间、文件描述符、打开的文件等资源。
  - **线程**：CPU 调度的最小单位。线程是进程内的执行流，共享进程的资源。

【详细区别对比】
| 维度 | 进程 | 线程 |
| :--- | :--- | :--- |
| **资源拥有** | 独立内存堆栈、全局变量、文件句柄。 | 共享所属进程的内存、文件句柄。拥有独立的栈 和 PC（程序计数器）。 |
| **通信方式** | IPC（管道、消息队列、共享内存、Socket、信号量）。 | 直接读写共享变量（需同步机制），或使用 wait/notify、Condition。 |
| **开销/切换** | 切换涉及保存/恢复整个地址空间（TLB 刷新），开销大。 | 切换只需保存少量寄存器（栈指针、PC），开销小。 |
| **健壮性** | 多进程间隔离性强，一个进程崩溃通常不影响其他进程。 | 一个线程崩溃（如非法内存访问）可能导致整个进程（所有线程）崩溃。 |
| **创建/销毁** | 复杂，涉及内存分配、初始化。 | 轻量，利用进程资源即可。 |

【实战案例】
Chrome 浏览器采用了多进程架构。每个 Tab 页、插件通常是独立的进程。这是因为浏览器需要加载不可信的第三方网页代码，如果某个网页崩溃（如内存溢出），多进程架构能保证浏览器主进程和其他 Tab 页不受影响，极大地提升了稳定性。如果是多线程架构，一个页面崩溃可能导致整个浏览器闪退。

【进程与线程的内存视图】
```text
┌────────────────── Process A ──────────────────┐
│  Code Segment, Data Segment, Heap (Shared)    │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ Thread 1│  │ Thread 2│  │ Thread 3│  ...   │
│  │   Stack │  │   Stack │  │   Stack │        │
│  └─────────┘  └─────────┘  └─────────┘        │
└────────────────────────────────────────────────┘

┌────────────────── Process B ──────────────────┐
│  Independent Memory Space                     │
│  (Cannot access Process A Memory directly)   │
└────────────────────────────────────────────────┘
```

【代码示例：Java 中线程隔离带来的数据可见性问题】
```java
class SharedData {
    // volatile 保证线程间可见性，否则线程可能无法看到主内存的更新
    static volatile boolean running = true;

    public static void main(String[] args) throws InterruptedException {
        new Thread(() -> {
            while (running) {
                // 循环等待
            }
            System.out.println("Thread stopped");
        }).start();

        Thread.sleep(1000);
        running = false; // 主线程修改值
        // 如果不加 volatile，子线程可能永远看不到 running 变为 false
    }
}
```

【关键补充细节】
- **上下文切换**：进程切换是“重型”切换，涉及 CR3 寄存器切换（页表），会导致 CPU Cache（L1/L2）失效；线程切换是“轻型”切换，Cache 热数据大概率还能保留。
- **协程**：用户态线程，由程序调度，切换开销更小，不涉及内核态切换（如 Java 21 的 Virtual Threads，Go Goroutines）。

## 常见考点
1. **线程共享了哪些资源，独享了哪些资源？**
   - 共享：堆内存、静态变量、打开的文件句柄、进程 ID (PID)。
   - 独享：栈、寄存器状态、程序计数器（PC）、线程 ID (TID)。
2. **多进程好还是多线程好？**
   - 高计算密集型且需隔离：多进程（如 Chrome 的多进程架构）。
   - 高 I/O 密集型且需频繁通信：多线程（开销小）。
3. **僵尸进程是什么？**：子进程结束但父进程未读取其退出状态码，子进程 PCB 残留，导致资源泄漏。
4. **零拷贝在进程通信中的作用？**：共享内存不需要数据在内核态和用户态之间拷贝，效率最高。

## 记忆要点

- 一句话定义：进程是资源分配的最小单位，而线程是 CPU 调度的最小单位。
- 资源对比：进程拥有独立内存，而线程共享所属进程的堆内存与文件句柄，仅独享栈和 PC。
- 开销对比：进程切换需刷新 TLB 开销极大，而线程切换开销极小。
- 健壮性对比：进程间内存强隔离互不影响，而线程一旦崩溃会导致整个进程崩溃。

## 结构化回答





**30 秒电梯演讲：** 进程像工厂，线程像工厂里的工人。多个工人共享工厂资源（线程共享内存），但不同工厂资源独立（进程隔离）。

**展开框架：**
1. **资源调度单位** — 资源分配与调度单位
2. **内存共享与隔离** — 内存共享与隔离
3. **开销与通信差异** — 开销与通信差异

**收尾：** 这是我实战中的理解，您想深入哪一段？





## 视频脚本

> 预计时长：3 分钟 | 由浅入深

| 时间 | 画面/字幕 | 口播台词 | 讲解要点 |
|------|----------|----------|----------|
| 0:00 | 标题卡：进程和线程的区别 | 今天这道题：进程和线程的区别。30 秒先给你讲清楚。 | 开场钩子 |
| 0:20 | 核心概念动画/示意图 | 进程像工厂，线程像工厂里的工人。多个工人共享工厂资源（线程共享内存），但不同工厂资源独立（进程隔离）。 | 核心概念 |
| 0:40 | 资源分配与调度单位示意图 | 资源分配与调度单位 | 资源分配与调度单位 |
| 1:10 | 总结卡 + 下期预告 | 记住今天这几个关键词，面试一定用得上。下期见。 | 收尾 |
